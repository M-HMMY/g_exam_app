import type { TextbookSection } from '../../types';

/** 技術分野：要素技術（1）画像を扱う層（categoryId: 'f-layer'）の節。 */
export const fLayer: TextbookSection[] = [
  {
    id: 'f-layer-1',
    categoryId: 'f-layer',
    title: '全結合層と、画像を扱うときの問題',
    goal: '全結合層の仕組みとパラメータ数を説明でき、画像で困る理由を言える',
    minutes: 15,
    body: `
# ざっくり言うと

ここから 2 つの節にわたって、**ディープラーニングを組み立てる部品のカタログ**を見ていきます。

この節の主役は**全結合層**です。[ニューラルネットワークの基本](textbook/e-dl-1)で見た「入力に重みを掛けて足す」層そのもので、**いちばん基本的な部品**です。

ただし、**画像を扱おうとすると、この層は 2 つの意味で行き詰まります。** その行き詰まりを知っておくと、次の節の畳み込み層が「**なぜそういう形なのか**」まで含めて理解できます。

# 全結合層：すべてのノードをつなぐ

**全結合層**は、**前の層のすべてのノードと、次の層のすべてのノードをつなぐ**層です。「全部つながっている」から全結合です。

\`\`\`diagram:flow
title: 全結合層がやっていること
前の層のすべての値を受け取る :: どの値も等しく見る
それぞれに重みを掛ける :: つながりごとに別々の重み
全部足してバイアスを加える :: ここまでが線形関数
活性化関数に通す :: 次の層へ渡す
\`\`\`

**「掛けて足す」だけの計算を線形関数といいます。** 全結合層の本体は線形関数で、[活性化関数](textbook/e-dl-2)と組んで初めて意味を持ちます。

## パラメータ数の数え方

**この試験で計算問題として出ます。** 数え方は単純です。

**入力が n 個、出力が m 個の全結合層のパラメータ数は、$n \\times m + m$。**

- **$n \\times m$** …… つながりの数だけ重みがある
- **$+ m$** …… 出力ノードごとにバイアスが 1 つずつある

たとえば、**入力 100 個、出力 50 個**なら、$100 \\times 50 + 50 = 5050$ 個です。

> **試験のポイント** **全結合層のパラメータ数は「入力数 × 出力数 + 出力数」。** 最後の「+ 出力数」はバイアスの分です。**問題文に「バイアスを含む」「含まない」の指定があればそれに従い、なければ含めて数える**のが基本です。

# 画像を全結合層に入れると、何が起きるか

さて、ここからが本題です。**画像をそのまま全結合層に入れると、2 つの問題が起きます。**

## 問題 1：パラメータが爆発する

カラー画像は、**縦 × 横 × 3（赤・緑・青）**の数値の集まりです。

**100 × 100 画素のカラー画像**なら、値の個数は

$100 \\times 100 \\times 3 = 30000$

**3 万個**です。これを入力として、**隠れ層のノードを 1000 個**置くとどうなるか。

$30000 \\times 1000 = 30000000$

**重みだけで 3000 万個。** これは 100 × 100 画素という、**とても小さな画像**での話です。実際に扱う画像はもっと大きく、**パラメータ数はすぐに手に負えなくなります。**

パラメータが多すぎると、**学習に膨大なデータと計算が必要になり、過学習もしやすくなります。**

## 問題 2：位置関係が失われる

こちらのほうが深刻です。

**全結合層に入れるとき、画像は 1 列に並べ直されます。** 縦 100 × 横 100 の並びを、端から順につないで 1 本の列にするわけです（色を無視して 1 枚ぶんで数えれば 1 万個、赤・緑・青の 3 枚ぶんなら 3 万個）。

\`\`\`diagram:compare
title: 画像を 1 列に並べ直すと失われるもの
actors: もとの画像 | 1 列に並べたあと
隣の画素は隣にある :: 上下左右のつながりが分かる
上下の画素が遠く離れる :: 1 行ぶん（100 個）離れてしまう
まとまりが目に見える :: 目・鼻・口が固まって見える
まとまりが分からなくなる :: ただの数値の列になる
\`\`\`

**画像の意味は「近くの画素どうしの関係」に宿っています。** 「この画素とその右隣の画素の明るさが急に変わる」から輪郭が見えるわけです。**1 列に並べ直すと、その手掛かりが消えます。**

もう 1 つ問題があります。**猫が画像の左上にいても右下にいても「猫」です。** ところが全結合層では、**左上の画素と右下の画素はまったく別の重みで扱われます。** 位置が変わっただけで、まるで別の入力になってしまいます。

> **よくある勘違い** 「全結合層でも、学習させれば画像を扱える」——理屈のうえでは可能ですが、**必要なデータ量が現実的でありません。** 「近くの画素どうしを見る」「位置が変わっても同じと見なす」という**当たり前の性質を、ゼロから学習させることになる**からです。

# だから、畳み込み層が要る

この 2 つの問題への答えが、**次の節の畳み込み層**です。

\`\`\`diagram:compare
title: 全結合層の問題と、畳み込み層の答え
actors: 全結合層の問題 | 畳み込み層の答え
パラメータが爆発する :: 画素の数だけ重みが要る
同じフィルタを使い回す :: 画像の大きさに関係なくパラメータ数が決まる
位置関係が失われる :: 1 列に並べ直してしまう
近くの画素どうしだけを見る :: 縦横の並びを保ったまま処理する
位置が変わると別物になる :: 左上と右下で重みが違う
位置が変わっても同じ特徴を捉える :: 同じフィルタを全体で動かす
\`\`\`

**「全画素を平等に見るのをやめる」。** これが、次の節を貫く一点です。

なお、**全結合層が不要になったわけではありません。** CNN でも、**最後に全結合層を置いて分類の答えを出す**構成が長く使われてきました。**畳み込み層で特徴を取り出し、全結合層で結論を出す**、という役割分担です。

# この節のまとめ

- **全結合層**は、前の層のすべてのノードと次の層のすべてのノードをつなぐ層。本体は**線形関数**
- **パラメータ数は「入力数 × 出力数 + 出力数」。** 最後の項はバイアス
- 画像を全結合層に入れると **2 つの問題**が起きる
- **問題 1：パラメータが爆発する。** 100 × 100 のカラー画像（3 万個）を 1000 ノードにつなぐと、重みだけで 3000 万個
- **問題 2：位置関係が失われる。** 1 列に並べ直すと、近くの画素どうしのつながりが消える。位置が変わると別の入力になる
- この 2 つへの答えが**畳み込み層**。「**全画素を平等に見るのをやめる**」
- **全結合層が不要になったわけではない。** 畳み込み層で特徴を取り出し、全結合層で結論を出す構成が長く使われてきた

\`\`\`quiz
全結合層とはどんな層ですか :: 前の層のすべてのノードと、次の層のすべてのノードをつなぐ層です。
入力 100 個、出力 50 個の全結合層のパラメータ数は :: 100 かける 50 に、バイアス 50 を足して 5050 個です。
100 かける 100 画素のカラー画像の入力値は何個ですか :: 縦 100、横 100、色 3 なので 3 万個です。
画像を全結合層に入れると起きる問題を 2 つ挙げてください :: パラメータが爆発することと、1 列に並べ直すために画素どうしの位置関係が失われることです。
位置関係が失われると、なぜ困るのですか :: 画像の意味は近くの画素どうしの関係に宿っているためです。並べ直すとその手掛かりが消えます。
全結合層では、猫が左上にいる場合と右下にいる場合をどう扱いますか :: まったく別の重みで扱うため、位置が変わるだけで別の入力になってしまいます。
CNN では全結合層はまったく使われないのですか :: いいえ。畳み込み層で特徴を取り出し、最後に全結合層で結論を出す構成が長く使われてきました。
\`\`\`
`,
  },

  {
    id: 'f-layer-2',
    categoryId: 'f-layer',
    title: '畳み込み層',
    goal: '畳み込みの仕組みを説明でき、出力サイズとパラメータ数を計算できる',
    minutes: 25,
    body: `
# ざっくり言うと

**畳み込み層**は、前の節の 2 つの問題に、**たったひとつの発想**で答えます。

**「小さな虫めがねを、画像の上でずらしながら見ていく」。**

虫めがねの大きさは 3 × 3 画素くらい。それを左上から右へ、下へと動かしながら、**その範囲だけを見て計算します。** 全画素を一度に見ることは、決してしません。

これで何が解決するか。

- **虫めがねは 1 枚しかない**ので、パラメータは虫めがねの大きさぶんだけ。**画像がどれだけ大きくても増えません**
- **近くの画素だけを見る**ので、位置関係が保たれます
- **同じ虫めがねを画像全体で使う**ので、猫が左上にいても右下にいても同じように見つかります

この虫めがねを**フィルタ**（またはカーネル）といいます。

そして、**この畳み込み層を中心に組み立てたネットワーク**を、**畳み込みニューラルネットワーク（CNN、Convolutional Neural Network）**といいます。画像を扱うディープラーニングは、長らくこの形が主流でした。

# 畳み込みの計算

やっていることは、**「フィルタの数値と、重なった画素の数値を掛けて、全部足す」**だけです。

\`\`\`diagram:flow
title: 畳み込みの 1 回ぶん
フィルタを画像の左上に重ねる :: 3 かける 3 の範囲を見る
重なった場所どうしを掛ける :: 9 組の掛け算
全部足す :: 1 つの数値になる
出力の 1 マスに書き込む :: これで 1 マス完成
フィルタを右へずらす :: 次のマスへ
\`\`\`

**フィルタの中身（重み）は、学習で決まります。** 人が「輪郭を検出するフィルタ」を設計するのではなく、**データから「輪郭を捉えると都合がよい」と学ぶ**わけです。

こうしてできた**出力の 2 次元の並び**を**特徴マップ**といいます。「どこにその特徴があったか」の地図です。

## フィルタは何枚も使う

フィルタ 1 枚では、**1 種類の特徴しか捉えられません。** 縦の輪郭を捉えるフィルタは、横の輪郭を見逃します。

そこで、**フィルタを何十枚も並べます。** 16 枚使えば、**特徴マップも 16 枚**できます。この枚数を**出力チャンネル数**といいます。

**「チャンネル数」は入力側にも出力側にもあります。** 入力がカラー画像なら**入力チャンネル数は 3**（赤・緑・青）、フィルタを 16 枚使えば**出力チャンネル数は 16**です。**次の層から見れば、この 16 が入力チャンネル数になります。**

# 3 つの設定

畳み込み層には、覚えるべき設定が 3 つあります。**この 3 つで出力の大きさが決まります。**

| 設定 | 意味 |
| --- | --- |
| **フィルタサイズ** | 虫めがねの大きさ。3 × 3、5 × 5 など |
| **ストライド** | **1 回に何マスずらすか。** 1 なら 1 マスずつ、2 なら 1 つ飛ばし |
| **パディング** | **画像の周りに余白（ふつう 0）を足すこと。** 何マス足すかを指定する |

## パディングは何のためにあるか

パディングがないと、**畳み込むたびに画像が小さくなります。** 3 × 3 のフィルタを通すと、縦横それぞれ 2 マスずつ縮みます。**層を重ねると、あっという間に消えてしまいます。**

もう 1 つ、**端の画素は中央の画素より見られる回数が少ない**という不公平もあります。

**周りに余白を足しておけば、この 2 つが解決します。** 出力サイズを入力と同じに保てるようにパディングを取ることを、よく行います。

# 出力サイズの計算

**この試験で計算問題として出ます。** 式は 1 つだけです。

出力サイズは、次の式で求まります。

\`\`\`math
\\frac{W - F + 2P}{S} + 1
\`\`\`

記号の意味を日本語にします。

- **$W$** …… 入力の 1 辺の大きさ
- **$F$** …… フィルタの 1 辺の大きさ
- **$P$** …… パディングの幅（片側）
- **$S$** …… ストライド

**「入力から、フィルタがはみ出す分を引き、余白を足し、ずらす幅で割って、1 を足す」**と読みます。最後の「+1」は、**最初の 1 回ぶん**です。

## 計算してみる

**例 1：** 入力 32 × 32、フィルタ 5 × 5、パディングなし、ストライド 1

$(32 - 5 + 0) \\div 1 + 1 = 27 + 1 = 28$

→ **出力は 28 × 28。** 縦横それぞれ 4 マス縮みました。

**例 2：** 入力 32 × 32、フィルタ 5 × 5、**パディング 2**、ストライド 1

$(32 - 5 + 4) \\div 1 + 1 = 31 + 1 = 32$

→ **出力は 32 × 32。** 入力と同じ大きさが保たれました。

**例 3：** 入力 32 × 32、フィルタ 2 × 2、パディングなし、**ストライド 2**

$(32 - 2 + 0) \\div 2 + 1 = 15 + 1 = 16$

→ **出力は 16 × 16。** ストライドを 2 にすると、およそ半分になります。

> **試験のポイント** **出力サイズは (W − F + 2P) / S + 1。** パディングは**両側**に付くので **2P** です。**片側の幅を 2 倍する**のを忘れないでください。

# パラメータ数の計算

こちらも出ます。**全結合層との違いが決定的**なので、必ず押さえてください。

**畳み込み層のパラメータ数は、**

**フィルタの縦 × フィルタの横 × 入力チャンネル数 × フィルタ枚数 ＋ フィルタ枚数（バイアス）**

## 計算してみる

**入力がカラー画像（3 チャンネル）、フィルタ 5 × 5、フィルタ 16 枚**の場合。

$5 \\times 5 \\times 3 \\times 16 + 16 = 1200 + 16 = 1216$

**わずか 1216 個**です。

**ここが決定的です。** この式に、**入力画像の大きさ（32 × 32 なのか 1000 × 1000 なのか）が入っていません。**

\`\`\`diagram:compare
title: 全結合層と畳み込み層のパラメータ数
actors: 全結合層 | 畳み込み層
画像の大きさに比例して増える :: 画素が増えれば重みも増える
画像の大きさに依存しない :: フィルタの大きさと枚数だけで決まる
場所ごとに別の重み :: 左上と右下で別々に学習する
同じフィルタを全体で使い回す :: 場所が変わっても同じ特徴を捉える
3000 万個になることもある :: 100 かける 100 の画像で隠れ層 1000 ノード
1216 個で足りる :: 5 かける 5 のフィルタ 16 枚、入力 3 チャンネル
\`\`\`

**同じフィルタを画像全体で使い回すこと**を**パラメータの共有**といい、**近くの画素だけを見ること**を**局所的な結合**といいます。**畳み込み層の利点は、この 2 つに集約されます。**

> **試験のポイント** **畳み込み層のパラメータ数は入力画像の大きさに依存しない。** これが全結合層との決定的な違いです。**「パラメータの共有」と「局所的な結合」**という 2 語で説明できるようにしてください。

\`\`\`widget:convolution
\`\`\`

# 畳み込みの変種

シラバスには、畳み込みの工夫が 3 つ載っています。**名前と目的の組**で覚えてください。

| 名前 | 何をするか | 何が良いか |
| --- | --- | --- |
| **Dilated Convolution**（**Atrous Convolution** ともいう） | フィルタの**要素のあいだに隙間をあける** | **パラメータを増やさずに、広い範囲を見られる** |
| **Depthwise Separable Convolution** | 畳み込みを**2 段階に分ける**（チャンネルごとの畳み込み → 1 × 1 の畳み込み） | **パラメータと計算量を大きく減らせる** |

**Dilated Convolution** は、3 × 3 のフィルタの各要素を 1 マスずつ離して置くようなものです。**見る範囲は 5 × 5 に広がるのに、パラメータは 9 個のまま**です。広い範囲の情報が要るセグメンテーションなどで使われます。

**Depthwise Separable Convolution** は、**軽量なモデルの鍵になる技術**です。ふつうの畳み込みは「空間方向」と「チャンネル方向」を同時にやりますが、**これを 2 段階に分けることで計算量を大きく減らします。** のちに出てくる **MobileNet** がこれを使っています。

> **試験のポイント** **Dilated（Atrous）Convolution は隙間をあけて広い範囲を見る、Depthwise Separable Convolution は 2 段階に分けて軽くする。** 目的が違うので、取り違えないでください。

# この節のまとめ

- **畳み込み層は「小さなフィルタを画像の上でずらしながら見る」。** 全画素を平等に見るのをやめる
- **フィルタ（カーネル）の中身は学習で決まる。** 人が設計するのではない
- 出力の 2 次元の並びが**特徴マップ**。フィルタを何枚も使い、枚数が**チャンネル数**になる
- 設定は 3 つ。**フィルタサイズ・ストライド（何マスずらすか）・パディング（周りの余白）**
- **パディングは、画像が縮むのを防ぎ、端の画素の不公平をなくす**ためにある
- **出力サイズ = (W − F + 2P) / S + 1。** パディングは両側に付くので **2P**
- **パラメータ数 = フィルタ縦 × フィルタ横 × 入力チャンネル数 × フィルタ枚数 + フィルタ枚数**
- **畳み込み層のパラメータ数は入力画像の大きさに依存しない。** 利点は**パラメータの共有**と**局所的な結合**
- **Dilated（Atrous）Convolution は隙間をあけて広い範囲を見る。Depthwise Separable Convolution は 2 段階に分けて軽くする**

\`\`\`quiz
畳み込み層の基本的な考え方は :: 小さなフィルタを画像の上でずらしながら、その範囲だけを見て計算していくことです。
フィルタの中身は誰が決めますか :: 学習で自動的に決まります。人が設計するわけではありません。
ストライドとパディングはそれぞれ何を指定しますか :: ストライドは 1 回に何マスずらすか、パディングは画像の周りに足す余白の幅です。
パディングは何のためにありますか :: 畳み込みのたびに画像が縮むのを防ぐことと、端の画素が見られる回数が少ない不公平をなくすためです。
出力サイズの式を言ってください :: 入力サイズからフィルタサイズを引き、パディングの 2 倍を足し、ストライドで割って 1 を足します。
入力 32、フィルタ 5、パディングなし、ストライド 1 のときの出力サイズは :: 32 引く 5 は 27、1 で割って 1 を足すので 28 です。
入力 32、フィルタ 5、パディング 2、ストライド 1 のときの出力サイズは :: 32 引く 5 足す 4 で 31、1 を足して 32 です。入力と同じ大きさが保たれます。
入力 3 チャンネル、フィルタ 5 かける 5 を 16 枚使うときのパラメータ数は :: 5 かける 5 かける 3 かける 16 で 1200、バイアス 16 を足して 1216 個です。
畳み込み層のパラメータ数は入力画像の大きさに依存しますか :: 依存しません。フィルタの大きさと枚数、入力チャンネル数だけで決まります。
畳み込み層の利点を 2 語で言うと :: パラメータの共有と、局所的な結合です。
Dilated Convolution は何をする手法ですか :: フィルタの要素のあいだに隙間をあけて、パラメータを増やさずに広い範囲を見られるようにする手法です。
Depthwise Separable Convolution の目的は :: 畳み込みを 2 段階に分けることで、パラメータと計算量を大きく減らすことです。
\`\`\`
`,
  },

  {
    id: 'f-layer-3',
    categoryId: 'f-layer',
    title: 'プーリング層と正規化層',
    goal: 'プーリングの役割と、代表的な正規化手法の違いを説明できる',
    minutes: 20,
    body: `
# ざっくり言うと

畳み込み層の周りには、**それを支える 2 種類の層**が置かれます。

- **プーリング層**……**「少しずれても同じと見なす」**ための層。情報を粗くまとめる
- **正規化層**……**「値の大きさをそろえる」**ための層。学習を安定させる

どちらも**主役ではありません**が、**これがないと深いネットワークは学習しません。** 縁の下の力持ちです。

# プーリング層：粗くまとめる

**プーリング層**は、**決められた範囲の値を 1 つにまとめて、特徴マップを小さくする**層です。

やり方は 2 通りあります。

| 名前 | やり方 |
| --- | --- |
| **最大値プーリング** | 範囲内の**最大値**を取る |
| **平均値プーリング** | 範囲内の**平均値**を取る |

2 × 2 の範囲を、**2 マスずつずらしながら（ストライド 2 で）**最大値プーリングすると、**4 マスが 1 マスになります。** 特徴マップは縦横それぞれ半分になります。

**重要な性質があります。**

> **試験のポイント** **プーリング層には学習するパラメータがありません。** 決められた規則（最大を取る、平均を取る）で計算するだけです。「プーリング層のパラメータを学習する」と書かれた選択肢は誤りです。

## 何のためにあるのか：不変性の獲得

プーリングの目的は、**単に小さくすることではありません。**

**「少し位置がずれても、同じ結果になるようにする」**ためです。これを**不変性の獲得**といいます。

\`\`\`diagram:flow
title: 最大値プーリングが位置のずれを吸収するしくみ
ある範囲に強い反応がある :: 「ここに輪郭がある」
その位置が 1 マスずれる :: 撮影角度が少し違うなど
それでも同じ範囲の中にある :: 2 かける 2 の枠の中での移動
最大値は変わらない :: 出力は同じになる
\`\`\`

猫の写真が 1 画素ずれただけで「猫ではない」と判定されては困ります。**プーリングは、この「多少のずれ」を吸収します。**

## グローバルアベレージプーリング（GAP）

**特徴マップ 1 枚を、まるごと 1 つの値にする**プーリングです。範囲を「マップ全体」にした平均値プーリング、と考えてください。

これが何の役に立つのか。**全結合層の代わりになります。**

\`\`\`diagram:compare
title: 最後に全結合層を置く場合と、GAP を使う場合
actors: 全結合層を置く | GAP を使う
特徴マップを 1 列に並べ直す :: たとえば 7 かける 7 かける 512 で 25088 個
特徴マップ 1 枚を 1 値にする :: 512 個になる
つなぐと膨大なパラメータが要る :: ここがモデル全体の大半を占めることもある
パラメータは増えない :: 平均を取るだけなので学習する重みがない
過学習しやすい :: パラメータが多いため
過学習しにくい :: パラメータが減る
\`\`\`

**GAP を使うとパラメータが大きく減り、過学習も抑えられます。** 近年のモデルの多くが、**最後に積み重ねていた大きな全結合層**を GAP に置き換えています（クラス数ぶんの全結合層を 1 枚だけ残す構成が一般的です）。

> **試験のポイント** **GAP は特徴マップ 1 枚を 1 つの値にまとめるもので、全結合層の代わりに使うとパラメータを大きく減らせる。** これが最大の利点です。

# 正規化層：値の大きさをそろえる

**正規化**とは、**値の散らばり方をそろえる**ことです。平均を 0 に、ばらつきを 1 にそろえる、といった処理をします。

なぜ必要なのか。**層を通るうちに、値の大きさがどんどん偏っていくから**です。ある層では 0.001 前後、別の層では 1000 前後——こうなると、**学習率をどう設定しても、どこかの層が壊れます。**

**正規化層を挟むと、各層に入ってくる値の大きさが安定し、学習が速く、安定します。**

\`\`\`diagram:flow
title: 正規化層があると何が変わるか
値の大きさが層ごとに偏る :: ある層は極端に大きく、ある層は極端に小さい
学習率をそろえられない :: どこかの層で発散するか、止まる
正規化層を挟む :: 各層に入る値の散らばりをそろえる
学習が速く安定する :: 大きめの学習率も使えるようになる
\`\`\`

## 4 種類の正規化

**「どの範囲でそろえるか」が違うだけ**です。**この違いが試験に出ます。**

| 名前 | どの範囲でそろえるか | 特徴 |
| --- | --- | --- |
| **バッチ正規化** | **ミニバッチ内の同じチャンネル**でそろえる | 最も基本。**バッチサイズが小さいと不安定になる** |
| **レイヤー正規化** | **1 つのサンプルの、全チャンネル**でそろえる | **バッチサイズに依存しない。** RNN や Transformer で使われる |
| **インスタンス正規化** | **1 つのサンプルの、1 チャンネルごと**にそろえる | 画像のスタイル変換などで使われる |
| **グループ正規化** | **1 つのサンプルの、チャンネルをいくつかの組に分けて**そろえる | バッチサイズが小さくても安定する |

**最も重要なのはバッチ正規化とレイヤー正規化の違い**です。

\`\`\`diagram:compare
title: バッチ正規化とレイヤー正規化
actors: バッチ正規化 | レイヤー正規化
ミニバッチ内でそろえる :: 複数のサンプルをまたいで計算する
1 つのサンプルの中でそろえる :: 他のサンプルを見ない
バッチサイズが小さいと不安定 :: 統計量が信頼できなくなる
バッチサイズに依存しない :: 1 件でも計算できる
画像の CNN でよく使われる :: 大きなバッチが取りやすい
RNN や Transformer で使われる :: 系列の長さが揃わなくても使える
\`\`\`

**バッチ正規化は「複数のサンプルをまたいで」統計を取ります。** だからバッチサイズが小さいと、**統計が当てにならず不安定になります。**

**レイヤー正規化は 1 つのサンプルの中だけで完結します。** だから**バッチサイズに左右されません。** 文章のような系列データを扱う **RNN**（前から順に読むネットワーク）や **Transformer**（いまの生成 AI の土台になっている構造）で使われるのは、この理由です。どちらも[要素技術（2）系列・生成・データ拡張](textbook/g-seq-1)以降で扱います。

**グループ正規化**は、バッチ正規化がバッチサイズに弱いという問題への対処として提案されたもので、**チャンネルをグループに分けてそろえます。**

> **試験のポイント** **バッチ正規化はミニバッチ内でそろえるのでバッチサイズが小さいと不安定。レイヤー正規化はサンプルごとなのでバッチサイズに依存せず、RNN や Transformer で使われる。** この対比が最もよく問われます。

> **よくある勘違い** **正規化層は、[前の章で見た正則化](textbook/e-dl-4)とは別のものです。** 名前が似ていますが、正則化は「過学習を防ぐために制限をかける」もの、正規化は「値の大きさをそろえて学習を安定させる」ものです。**目的が違います。**（なお、バッチ正規化には副次的に過学習を抑える効果もあるとされます。）

# この節のまとめ

- **プーリング層**は決められた範囲の値を 1 つにまとめる層。**最大値プーリング**と**平均値プーリング**がある
- **プーリング層には学習するパラメータがない**
- 目的は**不変性の獲得**（少し位置がずれても同じ結果になるようにする）
- **グローバルアベレージプーリング（GAP）**は特徴マップ 1 枚を 1 つの値にする。**積み重ねた大きな全結合層の代わりに使うとパラメータを大きく減らせる**
- **正規化層**は値の散らばり方をそろえて、**学習を速く安定させる**
- **バッチ正規化はミニバッチ内でそろえる。** バッチサイズが小さいと不安定
- **レイヤー正規化は 1 つのサンプルの中でそろえる。** バッチサイズに依存せず、**RNN や Transformer で使われる**
- **インスタンス正規化**はサンプルの 1 チャンネルごと、**グループ正規化**はチャンネルを組に分けてそろえる
- **正規化と正則化は別物。** 正規化は値をそろえる、正則化は過学習を防ぐ

\`\`\`quiz
プーリング層は何をする層ですか :: 決められた範囲の値を 1 つにまとめて、特徴マップを小さくする層です。
プーリング層に学習するパラメータはありますか :: ありません。最大を取る、平均を取るという決められた規則で計算するだけです。
プーリングの目的である「不変性の獲得」とは何ですか :: 少し位置がずれても同じ結果になるようにすることです。
グローバルアベレージプーリングとは何ですか :: 特徴マップ 1 枚をまるごと 1 つの値にまとめるプーリングです。積み重ねた大きな全結合層の代わりに使うとパラメータを大きく減らせます。
正規化層は何のためにありますか :: 層を通るうちに偏っていく値の大きさをそろえ、学習を速く安定させるためです。
バッチ正規化はどの範囲でそろえますか :: ミニバッチ内の同じチャンネルでそろえます。複数のサンプルをまたいで統計を取ります。
バッチ正規化の弱点は :: バッチサイズが小さいと統計が当てにならず、不安定になることです。
レイヤー正規化はどの範囲でそろえますか :: 1 つのサンプルの中の全チャンネルでそろえます。他のサンプルを見ないのでバッチサイズに依存しません。
RNN や Transformer でレイヤー正規化が使われるのはなぜですか :: バッチサイズや系列の長さに左右されずに計算できるからです。
正規化と正則化はどう違いますか :: 正規化は値の大きさをそろえて学習を安定させるもの、正則化は過学習を防ぐために制限をかけるものです。
\`\`\`
`,
  },

  {
    id: 'f-layer-4',
    categoryId: 'f-layer',
    title: 'スキップ結合と ResNet',
    goal: 'スキップ結合が何を解決したかを説明できる',
    minutes: 15,
    body: `
# ざっくり言うと

ここまでの部品がそろって、**層を深くする準備は整いました。** ところが、実際に深くしてみると**おかしなことが起きました。**

**層を増やしたのに、精度が下がったのです。**

過学習なら、訓練データでの精度は上がるはずです。ところが**訓練データでの精度まで下がりました。** つまり**そもそも学習ができていない。** これを**劣化**（degradation）といいます。

この問題を、**驚くほど単純な工夫**で解いたのが**スキップ結合**です。

# 深くすると精度が下がる、という不可解

理屈のうえでは、**層を増やして精度が下がるはずはありません。**

20 層のネットワークに 10 層足して 30 層にしたとします。**足した 10 層が「何もしない（入力をそのまま出す）」ように学習すれば、20 層と同じ性能になるはず**です。それより悪くなるのは変です。

\`\`\`diagram:compare
title: 理屈と、実際に起きたこと
actors: 理屈のうえでは | 実際に起きたこと
深くすれば同じか良くなるはず :: 足した層が何もしなければ元と同じ
深くすると悪くなった :: 訓練データでの精度まで下がった
足した層は何もしないよう学習できるはず :: 恒等変換を学べばよい
「何もしない」の学習が難しかった :: そこが盲点だった
\`\`\`

**原因は、最後の行にあります。** 「何もしない」——つまり**入力をそのまま出す変換**を、重みの調整だけで作るのは、**実は難しい**のです。

# スキップ結合：入力をそのまま先へ渡す道を作る

**スキップ結合**の発想は単純です。**「何もしない」を学習させるのが難しいなら、最初から「何もしない道」を用意しておけばいい。**

\`\`\`diagram:flow
title: スキップ結合のしくみ
入力が来る :: ここで道が 2 つに分かれる
片方は層を通る :: ふつうに畳み込みなどの処理をする
もう片方はそのまま先へ :: 何も処理せずに飛び越える
2 つを合流させて足す :: 処理した結果と、元の入力を足し合わせる
\`\`\`

**層をまるごと飛び越える近道（ショートカット）を作る**わけです。

こうすると何が起きるか。**層が「何もしない」ようにしたければ、層の出力を 0 にすればよくなります。** 入力はスキップ結合を通ってそのまま届くからです。**「入力をそのまま出す」を学習するより、「0 を出す」を学習するほうがずっと簡単**です。

もう 1 つ、**大きな副産物**があります。**逆伝播のときも、この近道を通って勾配がまっすぐ流れます。** [連鎖律で掛け算が重なって勾配が消える問題](textbook/e-dl-5)が、大きく緩和されます。

> **試験のポイント** **スキップ結合が解決したのは、層を深くすると精度が下がる「劣化」の問題。** 副次的に**勾配が届きやすくなる**効果もあります。「過学習を防ぐための手法」と書かれた選択肢は誤りです。

# ResNet：100 層を超えた

**スキップ結合を全面的に採用したモデル**が **ResNet** です。**2015 年、マイクロソフトリサーチの何恺明（カイミング・ヘー）らが発表**しました。

**Res** は **Residual（残差）**の略です。層が学習するのが「出力そのもの」ではなく、**「入力からの差分（残差）」**になるため、この名前が付いています。

ResNet は **ILSVRC 2015 で優勝**し、**152 層**という、それまで考えられなかった深さを実現しました。

\`\`\`diagram:stack
title: 代表的な CNN の層の深さ
top: 深い
bottom: 浅い
ResNet（2015 年） :: 152 層。スキップ結合で深さの壁を越えた。ILSVRC 優勝
GoogLeNet（2014 年） :: 22 層。ILSVRC の画像分類で優勝
VGG（2014 年） :: 16 層または 19 層。画像分類は 2 位
AlexNet（2012 年） :: 8 層。ILSVRC 優勝
\`\`\`

**8 層から 152 層へ。** この飛躍を可能にしたのがスキップ結合でした。

> **よくある勘違い** **スキップ結合は「層を減らす」工夫ではありません。** 層を飛び越える道を**追加する**工夫であり、層そのものはそのままです。処理した結果と、飛び越えてきた入力を**足し合わせて**先へ渡します。

**スキップ結合の考え方は、いまでは画像以外にも広く使われています。** [Attention と Transformer](textbook/g-seq-2)で扱う Transformer にも、同じ発想の仕組みが組み込まれています。

# この節のまとめ

- 層を深くすると**訓練データでの精度まで下がる**現象が起きた。これを**劣化**という
- 原因は、**「何もしない（入力をそのまま出す）」変換を学習するのが難しい**こと
- **スキップ結合**は、**層を飛び越えて入力をそのまま先へ渡す道**を作る工夫。処理結果と入力を**足し合わせる**
- これにより「何もしない」は「層の出力を 0 にする」で済むようになり、**学習しやすくなった**
- 副次的に、**逆伝播のときに勾配がまっすぐ流れる**ので、勾配消失も緩和される
- **ResNet（2015 年、マイクロソフトリサーチ）**がスキップ結合を全面採用し、**152 層**を実現して ILSVRC で優勝した
- **Res は Residual（残差）の略。** 層が学習するのは出力そのものではなく、**入力からの差分**
- スキップ結合の考え方は、いまでは Transformer など画像以外にも広く使われている

\`\`\`quiz
層を深くすると起きた「劣化」とはどんな現象ですか :: 層を増やしたのに、訓練データでの精度まで下がってしまう現象です。過学習とは別の問題です。
劣化が起きる原因は何だと考えられましたか :: 足した層が「何もしない」つまり入力をそのまま出す変換を学習するのが、実は難しかったことです。
スキップ結合とはどんな仕組みですか :: 層を飛び越えて入力をそのまま先へ渡す道を作り、層の処理結果と足し合わせる仕組みです。
スキップ結合があると、なぜ「何もしない」が簡単になるのですか :: 入力は飛び越えてそのまま届くので、層の出力を 0 にするだけで「何もしない」が実現できるからです。
スキップ結合の副次的な効果は :: 逆伝播のときに勾配が近道を通ってまっすぐ流れるため、勾配消失問題が大きく緩和されます。
ResNet はいつ、どこが発表したモデルですか :: 2015 年にマイクロソフトリサーチが発表し、ILSVRC で優勝しました。
ResNet の Res は何の略ですか :: Residual、つまり残差の略です。層が学習するのが入力からの差分になるためです。
ResNet は何層のモデルでしたか :: 152 層です。それまでの AlexNet が 8 層、VGG が 16 層前後だったことを考えると大きな飛躍でした。
スキップ結合は層を減らす工夫ですか :: 違います。層を飛び越える道を追加する工夫で、層そのものはそのまま残ります。
\`\`\`
`,
  },
];
