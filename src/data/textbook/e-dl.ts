import type { TextbookSection } from '../../types';

/** 技術分野：ディープラーニングの概要（categoryId: 'e-dl'）の節。 */
export const eDl: TextbookSection[] = [
  {
    id: 'e-dl-1',
    categoryId: 'e-dl',
    title: 'ニューラルネットワークの基本',
    goal: '入力層・隠れ層・出力層の役割と、GPU が使われる理由を説明できる',
    minutes: 20,
    body: `
# ざっくり言うと

この章では、**ニューラルネットワークがどうやって学習するのか**、その中身を開けます。

先に全体を言ってしまいます。**やっていることは 3 手順だけ**です。

\`\`\`diagram:flow
title: この章の全体（学習の 3 手順）
予測する :: 入力を受け取って、答えを出す
どれだけ外れたか測る :: 正解とのズレを数値にする
外れが減る向きにパラメータをずらす :: 少しだけ動かす
\`\`\`

そして、**この章に出てくる部品は、すべてこの 3 手順のどこかを担当しています。**

| 節 | 担当する手順 |
| --- | --- |
| ニューラルネットワークの基本・活性化関数 | **予測する** |
| 誤差関数 | **どれだけ外れたか測る** |
| 誤差逆伝播法・最適化手法 | **外れが減る向きにずらす** |
| 正則化 | 3 手順を回すときの**行きすぎを抑える** |

**この教本でいちばん数式が出る章**ですが、[数式の読み方](textbook/i-3)の範囲を超えません。**解かせません。** 出てきた式は必ず日本語に置き換えます。

# 1 個の計算単位がやっていること

ニューラルネットワークは、**人間の神経回路に着想を得て**作られました。神経細胞は、複数の細胞から信号を受け取り、**その合計がある大きさを超えたら**次へ信号を出します。

**この「受け取って、重みを付けて足して、変換して出す」を数式にしたもの**が、ネットワークの 1 つ 1 つの計算単位（ノード）です。

\`\`\`diagram:flow
title: ノード 1 個の動き
複数の入力を受け取る :: 前の層から値が来る
それぞれに重みを掛ける :: 重要な入力ほど大きい重みが掛かる
全部足す :: バイアスという下駄も足す
活性化関数に通す :: 変換して次へ渡す
\`\`\`

ここで出てきた**重み**と**バイアス**が、**学習で調整される値**です。つまり**パラメータ**です。「モデルを学習する」とは、**この重みとバイアスの値を決めること**にほかなりません。

> **よくある勘違い** **ニューラルネットワークは脳の再現ではありません。** 着想を得ただけで、実際の脳の働きとは大きく違います。「脳と同じ仕組みで動いている」と書かれた選択肢は誤りです。

# 単純パーセプトロンと、その限界

**単純パーセプトロン**は、**入力層と出力層だけ**でできた、最も単純なネットワークです。1958 年に**フランク・ローゼンブラット**が提案しました。

これは**直線 1 本でデータを分ける**ことしかできません。**線形分離可能**な問題だけが解ける、という言い方をします。

そして 1969 年、**マービン・ミンスキーらが「単純パーセプトロンは XOR すら解けない」**ことを指摘しました。

\`\`\`diagram:compare
title: 直線で分けられる問題と、分けられない問題
actors: 分けられる（AND や OR） | 分けられない（XOR）
1 本の直線で 2 つに分かれる :: 単純パーセプトロンで解ける
どう直線を引いても分かれない :: 単純パーセプトロンでは解けない
\`\`\`

XOR は「どちらか一方だけが 1 のとき 1」という、**きわめて単純な論理**です。**それすら解けない**という指摘は決定的で、**ニューラルネットワークの研究をその後長く停滞させました。**（なお、第 1 次ブーム全体が冬を迎えた理由は[3 度のブームと 2 度の冬](textbook/b-trend-1)で見たトイ・プロブレムと組合せ爆発です。ここで止まったのはニューラルネットワークの研究のほうです。）

# 多層パーセプトロン：層を重ねる

解決策は単純でした。**入力層と出力層のあいだに、もう 1 層はさむ**のです。

はさんだ層を**隠れ層**（中間層）といいます。外から見えないので「隠れ」です。

\`\`\`diagram:stack
title: ニューラルネットワークの層
top: 出力側
bottom: 入力側
出力層 :: 最終的な答えを出す。分類ならクラス数だけノードを置く
隠れ層 :: 入力層と出力層のあいだ。ここが深いのがディープラーニング
入力層 :: データをそのまま受け取る。特徴量の数だけノードを置く
\`\`\`

**隠れ層を持つものを多層パーセプトロン**といい、**これなら XOR も解けます。** 直線ではなく、**折れ曲がった境界**を引けるようになるからです。

**そして、この隠れ層を何層も重ねたものがディープラーニング**です。「ディープ（深い）」とは、**隠れ層が多い**という意味です。

> **試験のポイント** **単純パーセプトロンは線形分離可能な問題しか解けず、XOR が解けない。多層パーセプトロン（隠れ層を持つ）なら解ける。** この対比が問われます。

# なぜ GPU が必要なのか

ディープラーニングの学習は、**とてつもない量の計算**を必要とします。ただし、その計算の中身は**単純な掛け算と足し算の繰り返し**です。

ここに、計算機の得意不得意が効いてきます。

\`\`\`diagram:compare
title: CPU と GPU の得意分野
actors: CPU | GPU
複雑な処理を順番にこなす :: 高性能なコアが少数
単純な処理を一斉にこなす :: そこそこのコアが多数
なんでもできる汎用の頭脳 :: OS もアプリも動かす
単純計算の並列に特化 :: もとは画像処理のための装置
順番に解く問題に強い :: 前の結果が次に必要な処理
同じ計算を大量に繰り返す処理に強い :: ディープラーニングがまさにこれ
\`\`\`

**たとえるなら、CPU は「大学教授が 1 人」、GPU は「小学生が 1000 人」**です。難問を 1 つ解くなら教授が速い。**単純な計算を 100 万個こなすなら、小学生 1000 人のほうが圧倒的に速い。**

ディープラーニングの学習は後者なので、**GPU が向いています。** GPU はもともと画面描画（画像処理）のための装置でしたが、**その並列計算の力が転用されました。**

さらに **TPU** は、**Google がディープラーニングのために開発した専用のプロセッサ**です。用途を絞ることで、GPU よりさらに効率よく計算できます。

| 装置 | 特徴 |
| --- | --- |
| **CPU** | 汎用。複雑な処理を順番にこなすのが得意 |
| **GPU** | 単純な計算の並列処理が得意。**もとは画像処理用** |
| **TPU** | **Google がディープラーニング専用に開発**したプロセッサ |

> **試験のポイント** **GPU がディープラーニングに向くのは、単純な計算を大量に並列でこなせるから。** そして**GPU はもともと画像処理のための装置**でした。**TPU は Google がディープラーニング用に開発した専用プロセッサ**です。

なお、**特に重いのは学習の場面**です。推論は学習より軽く、**軽量化したモデルならスマートフォンの中でも動きます。**

ただし、**「推論はいつでも軽い」わけではありません。** 大規模言語モデルのように巨大なモデルでは、推論にも GPU や TPU を大量に使います（2026 年 9 月時点）。**この「重さをどう下げるか」が、のちの「モデルの軽量化」「エッジ AI」の話につながります。**

# この節のまとめ

- 学習の中身は **3 手順**（予測する → 外れを測る → 外れが減る向きにずらす）。この章の部品はすべてこのどこかを担当する
- ノード 1 個は「**入力に重みを掛けて足し、活性化関数に通して出す**」。**重みとバイアスがパラメータ**
- **ニューラルネットワークは脳の再現ではない。** 着想を得ただけ
- **単純パーセプトロン**は入力層と出力層だけ。**線形分離可能な問題しか解けず、XOR が解けない**
- **多層パーセプトロン**は**隠れ層**を持ち、XOR も解ける。**隠れ層を何層も重ねたのがディープラーニング**
- **GPU が向くのは、単純な計算を大量に並列でこなせるから。** **もともとは画像処理用の装置**
- **TPU は Google がディープラーニング専用に開発**したプロセッサ
- **特に重いのは学習の場面。** 推論は学習より軽いが、**巨大なモデルでは推論にも GPU や TPU が要る**（2026 年 9 月時点）

\`\`\`quiz
ニューラルネットワークのノード 1 個は何をしていますか :: 複数の入力に重みを掛けて足し、バイアスを加えてから活性化関数に通して次へ渡しています。
学習で調整されるパラメータとは具体的に何ですか :: ノード間のつながりの強さである重みと、各ノードが持つバイアスです。
単純パーセプトロンの限界は何ですか :: 直線 1 本でしか分けられないため、線形分離可能な問題しか解けず、XOR が解けません。
XOR が解けるようにするにはどうしますか :: 入力層と出力層のあいだに隠れ層をはさみ、多層パーセプトロンにします。
ディープラーニングの「ディープ」とは何が深いのですか :: 隠れ層が多い、という意味です。
GPU がディープラーニングに向くのはなぜですか :: 単純な計算を大量に並列でこなせるからです。ディープラーニングの計算は単純な掛け算と足し算の繰り返しです。
GPU はもともと何のための装置でしたか :: 画面描画などの画像処理のための装置で、その並列計算の力が転用されました。
TPU とは何ですか :: Google がディープラーニングのために開発した専用のプロセッサです。
大量の計算資源が必要になるのは学習と推論のどちらですか :: 特に重いのは学習です。推論は学習より軽いものの、巨大なモデルでは推論にも GPU や TPU が要ります。
\`\`\`
`,
  },

  {
    id: 'e-dl-2',
    categoryId: 'e-dl',
    title: '活性化関数',
    goal: '代表的な活性化関数の性質を説明でき、勾配消失問題との関係を言える',
    minutes: 20,
    body: `
# ざっくり言うと

前の節で、ノードは「入力に重みを掛けて足し、**活性化関数に通して**出す」と書きました。

**この活性化関数がないと、ディープラーニングは成立しません。** 層を何枚重ねても、**1 枚と同じ**になってしまうからです。

理由は簡単です。**掛けて足すだけの処理を何回繰り返しても、結局は「掛けて足す」1 回にまとめられます。** 5 に 2 を掛けて 3 を掛けても、6 を掛けたのと同じ。それでは層を重ねる意味がありません。

**あいだに「まっすぐでない変換」を入れることで、初めて層が意味を持ちます。** それが活性化関数の役割です。

> **試験のポイント** **活性化関数がないと、層を何枚重ねても 1 枚の線形変換と同じになる。** これが活性化関数が必要な理由です。ここは頻出です。

# 代表的な活性化関数

**この試験では、値域と性質が問われます。** 式は覚えなくて構いませんが、**値域と、微分の最大値**は押さえてください。

| 名前 | 出力の範囲 | 特徴 |
| --- | --- | --- |
| **シグモイド関数** | **0 から 1** | なめらか。**微分の最大値は 0.25** |
| **tanh 関数** | **−1 から 1** | シグモイドを上下に引き伸ばして中心を 0 にした形。**微分の最大値は 1** |
| **ReLU 関数** | **0 以上** | 負なら 0、正ならそのまま。**正の側で微分は 1** |
| **Leaky ReLU 関数** | 制限なし | 負の側にもわずかな傾きを持たせた ReLU |
| **ソフトマックス関数** | 0 から 1 で**総和が 1** | 多クラス分類の**出力層**で使う |

## シグモイド関数

**どんな値も 0 から 1 のあいだに押し込める**関数です。S 字の形をしています。

出力が 0〜1 なので**確率として読める**のが利点で、[ロジスティック回帰](textbook/d-ml-1)でも使われていました。

**ただし、隠れ層の活性化関数としては、いまはほとんど使われません。** 理由は次の節で説明する勾配消失問題です。

## tanh 関数

シグモイドとよく似た S 字ですが、**出力が −1 から 1** です。

シグモイドとの違いは 2 つ。**出力の中心が 0 になる**ことと、**微分の最大値が 1（シグモイドは 0.25）**であることです。**そのぶん勾配消失が起きにくく**、シグモイドより改善されています。

## ReLU 関数

**画像認識や一般的なネットワークで最も広く使われている活性化関数**です（2026 年 9 月時点）。中身は驚くほど単純で、

- **入力が 0 以下なら、出力は 0**
- **入力が 0 より大きければ、そのまま出す**

これだけです。**「負なら 0、正ならそのまま」**と覚えてください。

なぜこれが強いのか。**正の側で微分がちょうど 1 になる**からです。1 なら、何回掛けても 1 のまま。**勾配が小さくなっていきません。**

\`\`\`diagram:compare
title: シグモイドと ReLU
actors: シグモイド関数 | ReLU 関数
出力は 0 から 1 :: なめらかな S 字
出力は 0 以上 :: 折れ線
微分の最大値は 0.25 :: 掛け続けると 0 に近づく
正の側で微分は 1 :: 掛けても小さくならない
勾配消失が起きやすい :: 深い層で学習が進まない
勾配消失が起きにくい :: 深い層でも学習が進む
\`\`\`

**ReLU にも弱点があります。** 入力が負のあいだは出力も微分も 0 なので、**一度そこに入ったノードが二度と学習しなくなる**ことがあります。

そこで、**負の側にもわずかな傾きを持たせた**のが **Leaky ReLU 関数**です。「leaky＝漏れのある」という名前は、**負の側にも少しだけ値を漏らす**ことから来ています。

## ソフトマックス関数

これだけは役割が違います。**多クラス分類の出力層で使う**関数です。

複数の出力を受け取り、**すべてを 0〜1 に収め、しかも合計をちょうど 1 にします。**

\`\`\`diagram:flow
title: ソフトマックス関数の役割
出力層から複数の値が出る :: 「猫らしさ 3.2」「犬らしさ 1.1」「鳥らしさ 0.4」
ソフトマックスに通す :: 全部を 0〜1 にして、合計を 1 にする
確率として読める :: 「猫 0.85、犬 0.11、鳥 0.04」
最も大きいものを答えにする :: 「猫」と判定
\`\`\`

**合計が 1 になるので、そのまま確率として扱えます。** これが多クラス分類の出力層に使われる理由です。

> **試験のポイント** **ソフトマックス関数の出力は総和が 1 になる**ので、多クラス分類の出力層で確率として扱えます。**隠れ層には使いません。**

# 勾配消失問題：なぜシグモイドが退いたか

**この試験で繰り返し問われる、最重要の論点**です。

学習では、出力側の誤差を入力側へさかのぼらせて、各パラメータの直し方を求めます（詳しくは[誤差逆伝播法の節](textbook/e-dl-5)）。このとき、**層をさかのぼるたびに、活性化関数の微分が掛け合わされます。**

ここで、シグモイドの**微分の最大値は 0.25** でした。**最大でも 0.25** です。

\`\`\`diagram:flow
title: 勾配消失問題が起きるしくみ
出力側の誤差から出発する :: ここは大きい値
1 層さかのぼる :: 最大 0.25 が掛かる
さらにさかのぼる :: また 0.25 が掛かる。0.0625 に
10 層さかのぼる :: 0.25 を 10 回掛けると、ほぼ 0 になる
入力側が学習しない :: 直すべき量が 0 なので、動かない
\`\`\`

0.25 を 10 回掛けると、**約 0.000001** です。**入力側の層には、直すべき量がまったく届きません。**

これが**勾配消失問題**です。**層を深くしたいのに、深くすると入力側が学習しない**——ディープラーニングが 2012 年まで実用にならなかった大きな理由のひとつです。

**ReLU が広まったのは、この問題を大きく緩和したから**です。正の側で微分が 1 なので、何回掛けても小さくなりません。

> **試験のポイント** **勾配消失問題の原因は、1 未満の値（シグモイドなら最大 0.25）を層の数だけ掛け合わせること。** ReLU は正の側で微分が 1 なので、この問題が起きにくくなります。**この因果を言葉で言えるようにしてください。**

> **よくある勘違い** 「ReLU を使えば勾配消失は完全になくなる」わけではありません。**大きく緩和されるだけ**です。層がきわめて深い場合には、別の工夫（[スキップ結合と ResNet](textbook/f-layer-4)、[プーリング層と正規化層](textbook/f-layer-3)で扱います）も必要になります。

\`\`\`widget:activation
\`\`\`

# この節のまとめ

- **活性化関数がないと、層を何枚重ねても 1 枚の線形変換と同じ**になる。これが必要な理由
- **シグモイド関数**は出力 0〜1、**微分の最大値は 0.25**
- **tanh 関数**は出力 −1〜1、**微分の最大値は 1**。シグモイドより勾配消失が起きにくい
- **ReLU 関数**は「負なら 0、正ならそのまま」。**正の側で微分が 1** なので勾配消失が起きにくく、広く使われている
- ReLU の弱点は、**負の側に入ったノードが学習しなくなる**こと。**Leaky ReLU** は負の側にもわずかな傾きを持たせて対処
- **ソフトマックス関数は出力の総和が 1** になる。**多クラス分類の出力層**で使う
- **勾配消失問題の原因は、1 未満の値を層の数だけ掛け合わせること。** ReLU で大きく緩和されるが、完全になくなるわけではない

\`\`\`quiz
活性化関数がないと何が起きますか :: 層を何枚重ねても 1 枚の線形変換と同じになり、深くする意味がなくなります。
シグモイド関数の出力の範囲と、微分の最大値は :: 出力は 0 から 1、微分の最大値は 0.25 です。
tanh 関数の出力の範囲と、微分の最大値は :: 出力はマイナス 1 から 1、微分の最大値は 1 です。
ReLU 関数はどんな関数ですか :: 入力が 0 以下なら 0 を、0 より大きければそのままの値を出す関数です。
ReLU が勾配消失に強いのはなぜですか :: 正の側で微分がちょうど 1 になるため、層をさかのぼって掛け合わせても値が小さくなっていかないからです。
ReLU の弱点と、その対処は :: 負の側に入ったノードが学習しなくなることです。負の側にもわずかな傾きを持たせた Leaky ReLU で対処します。
ソフトマックス関数はどこで使い、どんな性質がありますか :: 多クラス分類の出力層で使い、出力の総和がちょうど 1 になるので確率として扱えます。
勾配消失問題の原因を説明してください :: 層をさかのぼるたびに 1 未満の値（シグモイドなら最大 0.25）が掛け合わされ、入力側では勾配がほぼ 0 になってしまうことです。
ReLU を使えば勾配消失は完全になくなりますか :: いいえ。大きく緩和されるだけで、層が非常に深い場合には別の工夫も必要です。
\`\`\`
`,
  },

  {
    id: 'e-dl-3',
    categoryId: 'e-dl',
    title: '誤差関数',
    goal: 'タスクに応じて適切な誤差関数を選べる',
    minutes: 18,
    body: `
# ざっくり言うと

学習の 3 手順のうち、**2 番目の「どれだけ外れたか測る」**を担当するのが**誤差関数**（損失関数）です。

**外れ具合の測り方は、1 つではありません。** そして、**何を測るかを間違えると、学習は正しい方向へ進みません。**

「家賃を当てる」のと「猫か犬かを当てる」のでは、**外れ方の意味が違います。** 家賃なら「3200 万円のところを 3000 万円と言った」という**距離**が意味を持ちます。分類なら「猫である確率を 0.2 と言った」という**確信度の外れ方**が問題です。

**だから誤差関数を使い分けます。** この節はその使い分けが中心です。

# 回帰には平均二乗誤差

**数値を当てる回帰**では、**平均二乗誤差関数**を使うのが基本です。

\`\`\`math
\\frac{1}{n} \\sum (y - \\hat{y})^2
\`\`\`

読み下すと、**「正解 $y$ と予測 $\\hat{y}$ のズレを 2 乗して全部足し、件数で割る」**。[数式の読み方](textbook/i-3)で見た式そのものです。

2 乗するのは 2 つの理由からです。

1. **プラスとマイナスが打ち消し合わないように**する
2. **大きく外れたときに、より強く罰する**（2 倍外れたら 4 倍の罰）

2 番目の性質には裏があります。**外れ値があると、そこに強く引っ張られます。** [MSE と MAE の違い](textbook/d-ml-6)で見たとおりです。

# 分類には交差エントロピー

**区分を当てる分類**では、**交差エントロピー**を使います。

考え方はこうです。**モデルは「猫である確率 0.85」のように確率を出します。** 正解が猫なら、0.85 と言ったのは悪くない。**0.01 と言ったなら、ひどく外れている。**

**交差エントロピーは、「正解のクラスに、どれだけ低い確率を割り当ててしまったか」を測ります。**

| モデルが「猫」に割り当てた確率 | 交差エントロピーの値 |
| --- | --- |
| 0.99 | とても小さい（ほぼ正解） |
| 0.50 | 中くらい |
| 0.01 | とても大きい（自信を持って間違えた） |
| 0 に近づく | 限りなく大きくなる |

ここに**log が使われる理由**があります。[数式の読み方](textbook/i-3)で「log は掛け算を足し算に変えるために付いている」と書きました。**分類でも同じ**です。各データが正解する確率を全部掛け合わせると値が小さくなりすぎるので、log を取って足し算にします。

そして、**この「正解する確率の積を最大にする」ことは、[最尤法](textbook/c-math-4)そのもの**です。**交差エントロピーを最小にすることと、尤度を最大にすることは同じことをしています。**

> **試験のポイント** **回帰には平均二乗誤差、分類には交差エントロピー。** この対応が基本です。「分類に平均二乗誤差を使う」と書かれた選択肢は、原則として誤りです。

# 2 つの分布の違いを測る：KL 情報量

**カルバック・ライブラー情報量（KL 情報量）**は、**2 つの確率分布がどれだけ違うか**を測る量です。

たとえば「モデルが出した確率分布」と「本当の確率分布」を比べる、という使い方をします。

**ここに落とし穴があります。**

> **よくある勘違い** **KL 情報量は「距離」ではありません。** 距離なら、A から B への距離と B から A への距離は同じはずです。**KL 情報量はそうなりません（対称ではありません）。** 「2 つの分布の距離を測る」と書かれた選択肢は誤りです。

「違いの大きさ」を測る量ではあるが、**距離の性質は満たしていない**——ここが問われます。

# 似ているかどうかを学ばせる誤差関数

分類とも回帰とも違う、**「2 つのものが同じか違うか」を学ばせたい**場面があります。

顔認証がその例です。**登録されていない人の顔も判定しなければならない**ので、「この人は A さん」と分類する形では作れません。代わりに、**「2 枚の顔写真が同一人物かどうか」**を判定できるようにします。

このために使われるのが、次の 2 つです。

\`\`\`diagram:compare
title: Contrastive Loss と Triplet Loss
actors: Contrastive Loss | Triplet Loss
2 つ 1 組で見る :: ペアを与える
3 つ 1 組で見る :: 基準・同じもの・違うものの 3 点を与える
同じなら近づけ、違うなら離す :: ペアごとに判断する
基準に対して、同じものを近く・違うものを遠くする :: 相対的な位置関係を学ぶ
\`\`\`

- **Contrastive Loss**……**2 つ 1 組**。同じクラスなら距離を縮め、違うクラスなら距離を広げる
- **Triplet Loss**……**3 つ 1 組**。基準（アンカー）に対して、**同じクラスのものを近づけ、違うクラスのものを遠ざける**

**Triplet Loss は「相対的な近さ」を学ばせる**ので、Contrastive Loss より安定するとされます。名前の「triplet」は 3 つ組という意味です。

> **試験のポイント** **Contrastive Loss は 2 つ 1 組、Triplet Loss は 3 つ 1 組。** この数の違いが問われます。どちらも「**似ているものは近く、似ていないものは遠く**」を学ばせる誤差関数です。

# この節のまとめ

- **誤差関数（損失関数）**は、学習の 3 手順のうち「**どれだけ外れたか測る**」を担当する
- **回帰には平均二乗誤差関数**。ズレを 2 乗して平均する。**大きく外れたときに強く罰する**ので、外れ値に引っ張られやすい
- **分類には交差エントロピー**。「正解のクラスにどれだけ低い確率を割り当てたか」を測る
- **交差エントロピーを最小にすることは、最尤法で尤度を最大にすることと同じ**
- **KL 情報量は 2 つの確率分布の違いを測る量。** ただし**対称ではないので「距離」ではない**
- **Contrastive Loss は 2 つ 1 組、Triplet Loss は 3 つ 1 組。** どちらも「似ているものは近く」を学ばせる

\`\`\`quiz
誤差関数は学習のどの手順を担当していますか :: 「どれだけ外れたか測る」手順です。予測と正解のズレを数値にします。
回帰と分類では、それぞれどの誤差関数を使うのが基本ですか :: 回帰には平均二乗誤差関数、分類には交差エントロピーを使います。
平均二乗誤差でズレを 2 乗する理由を 2 つ挙げてください :: プラスとマイナスが打ち消し合うのを防ぐためと、大きく外れたときにより強く罰するためです。
交差エントロピーは何を測っていますか :: 正解のクラスに、モデルがどれだけ低い確率を割り当ててしまったかを測っています。
交差エントロピーと最尤法の関係は :: 交差エントロピーを最小にすることと、尤度を最大にすることは同じことをしています。
KL 情報量は何を測る量ですか :: 2 つの確率分布がどれだけ違うかを測る量です。
KL 情報量は距離ですか :: 違います。A から B と B から A で値が変わる（対称ではない）ため、距離の性質を満たしていません。
Contrastive Loss と Triplet Loss の違いは :: Contrastive Loss は 2 つ 1 組で扱い、Triplet Loss は基準・同じもの・違うものの 3 つ 1 組で扱います。
顔認証で分類の形にしないのはなぜですか :: 登録されていない人も判定する必要があるためです。代わりに 2 枚が同一人物かを判定できるように学習させます。
\`\`\`
`,
  },

  {
    id: 'e-dl-4',
    categoryId: 'e-dl',
    title: '正則化',
    goal: '過学習を防ぐ代表的な手法を説明でき、L1 と L2 の違いを言える',
    minutes: 18,
    body: `
# ざっくり言うと

ディープラーニングのモデルは、**パラメータが非常に多い**のが特徴です。多ければ表現力は上がりますが、**訓練データを丸暗記できてしまう**ことも意味します。つまり[過学習](textbook/d-ml-6)です。

**正則化**は、**モデルが複雑になりすぎないよう、わざと制限をかける**工夫の総称です。

**「わざと足を引っ張る」**と言うと変に聞こえますが、これでうまくいきます。受験生に「過去問を丸暗記してはいけない」と縛りをかけると、**かえって本質を掴む**のと同じ理屈です。

# パラメータの大きさに罰を与える

いちばん基本的な考え方は、**「パラメータが大きくなりすぎたら罰点を課す」**ことです。

なぜパラメータの大きさが問題なのか。**パラメータが大きいと、入力のわずかな違いが出力の大きな違いになります。** つまり**訓練データの細かいノイズにまで反応する**モデルになってしまいます。

そこで、**誤差関数に「パラメータの大きさ」を足しておきます。** すると学習は、**誤差を減らしつつ、パラメータも小さく保とうとします。**

罰の与え方が 3 通りあり、**この違いが試験に出ます。**

| 名前 | 罰の与え方 | 別名 | 効果 |
| --- | --- | --- | --- |
| **L0 正則化** | **0 でないパラメータの個数**に罰 | — | 理想的だが**最適化が難しく、実用しにくい** |
| **L1 正則化** | パラメータの**絶対値の合計**に罰 | **ラッソ回帰** | **パラメータがちょうど 0 になりやすい** |
| **L2 正則化** | パラメータの**2 乗の合計**に罰 | **リッジ回帰** | **0 に近づくが 0 にはなりにくい** |

## L1 と L2 の違い

**この対応の取り違えが、この章で最も多い誤りです。**

\`\`\`diagram:compare
title: L1 正則化と L2 正則化
actors: L1 正則化（ラッソ回帰） | L2 正則化（リッジ回帰）
絶対値の合計に罰を与える :: 大きさに比例した罰
2 乗の合計に罰を与える :: 大きいほど急激に重い罰
パラメータがちょうど 0 になりやすい :: 使われない特徴量が出る
0 に近づくが 0 にはなりにくい :: 全体的に小さくなる
特徴選択の効果がある :: 不要な特徴量を自動で切り捨てる
すべての特徴量を少しずつ使う :: 切り捨てはしない
\`\`\`

**L1 の「ちょうど 0 になる」という性質**が重要です。パラメータが 0 になった特徴量は、**実質的に使われなくなります。** つまり **L1 正則化には特徴選択の効果があります。**

「たくさんある特徴量のうち、本当に効いているものだけ残したい」というときに L1 を選びます。

一方 **L2 は、全体をなだらかに小さくします。** 特定の特徴量を切り捨てず、**すべてを少しずつ使う**モデルになります。ディープラーニングでは L2 のほうがよく使われます。

> **試験のポイント** **L1 正則化＝ラッソ回帰＝パラメータが 0 になりやすい（スパースになる／特徴選択の効果がある）。L2 正則化＝リッジ回帰＝0 に近づくが 0 にはなりにくい。** この 2 組の対応を、**名前・別名・効果の 3 点セット**で覚えてください。

**L0 正則化**は、「0 でないパラメータの個数そのもの」に罰を与えるもので、**やりたいことに最も忠実**です。ただし**個数は連続的に変化しないため、勾配を使った最適化ができません。** そのため実用では L1 で代用します。

# ドロップアウト：わざとノードを休ませる

ディープラーニング特有の、まったく違う発想の正則化が**ドロップアウト**です。

**学習のたびに、ノードをランダムに何割か無効化します。**

\`\`\`diagram:flow
title: ドロップアウトの動き
学習の 1 回ごとに :: ノードをランダムに選ぶ
選んだノードを無効にする :: そのノードは存在しないものとして計算する
残りのノードだけで学習する :: 毎回、違う形のネットワークになる
推論のときは無効化しない :: すべてのノードを使う
\`\`\`

**なぜこれが効くのか。** 2 つの説明がされます。

1. **特定のノードに頼れなくなる。** 「あのノードが判定してくれるから自分は適当でいい」ができなくなり、**各ノードが自力で意味のある特徴を捉えるようになる**
2. **毎回違う形のネットワークになるので、実質的にたくさんの異なるモデルを学習し、まとめて使っていることになる。** [アンサンブル学習](textbook/d-ml-3)に似た効果がある

> **試験のポイント** **ドロップアウトは学習時にだけノードを無効化し、推論時には無効化しません。** 「推論時もランダムに無効化する」と書かれた選択肢は誤りです。

# 正則化以外の対処もある

過学習への対処は、正則化だけではありません。**全体を整理しておきます。**

\`\`\`diagram:tree
title: 過学習への対処
モデルを制限する :: 複雑になりすぎないようにする
  L1・L2 正則化 :: パラメータの大きさに罰を与える
  ドロップアウト :: ノードをランダムに無効化する
  早期終了 :: 悪化し始めたら学習を止める（最適化手法の節で扱う）
データを増やす :: 丸暗記できないほど例を与える
  データを集める :: 最も効果的だが費用がかかる
  データ拡張 :: 手元のデータを加工して水増しする（要素技術の章で扱う）
\`\`\`

**「データを増やす」が最も効果的**ですが、費用と時間がかかります。だからこそ正則化の工夫が発達しました。

# この節のまとめ

- **正則化**は、モデルが複雑になりすぎないよう、わざと制限をかける工夫の総称。目的は**過学習を防ぐこと**
- パラメータが大きいと**入力のわずかな違いに過剰に反応する**ので、大きさに罰を与える
- **L1 正則化＝ラッソ回帰。絶対値の合計に罰を与え、パラメータがちょうど 0 になりやすい（特徴選択の効果）**
- **L2 正則化＝リッジ回帰。2 乗の合計に罰を与え、0 に近づくが 0 にはなりにくい**
- **L0 正則化**は 0 でないパラメータの個数に罰を与えるが、**最適化が難しく実用しにくい**
- **ドロップアウトは学習時にノードをランダムに無効化する。推論時には無効化しない**
- ドロップアウトが効く理由は、**特定のノードに頼れなくなること**と、**アンサンブルに似た効果**
- 過学習への対処は正則化だけでなく、**データを増やす**方法もある（最も効果的だが費用がかかる）

\`\`\`quiz
正則化は何のために行いますか :: モデルが複雑になりすぎて訓練データを丸暗記する過学習を防ぐためです。
パラメータが大きいと、なぜ過学習しやすいのですか :: 入力のわずかな違いが出力の大きな違いになり、訓練データの細かいノイズにまで反応してしまうからです。
L1 正則化の別名と効果は :: 別名はラッソ回帰で、パラメータがちょうど 0 になりやすく、特徴選択の効果があります。
L2 正則化の別名と効果は :: 別名はリッジ回帰で、パラメータは 0 に近づきますが 0 にはなりにくく、全体がなだらかに小さくなります。
不要な特徴量を自動で切り捨てたいときは L1 と L2 のどちらですか :: L1 正則化です。パラメータがちょうど 0 になるため、その特徴量が使われなくなります。
L0 正則化が実用しにくいのはなぜですか :: 0 でないパラメータの個数は連続的に変化しないため、勾配を使った最適化ができないからです。
ドロップアウトとはどんな手法ですか :: 学習のたびにノードをランダムに何割か無効化して学習させる手法です。
ドロップアウトは推論のときも無効化しますか :: いいえ。無効化するのは学習時だけで、推論時はすべてのノードを使います。
ドロップアウトが効く理由を 1 つ挙げてください :: 特定のノードに頼れなくなり、各ノードが自力で意味のある特徴を捉えるようになるからです。毎回違う形になるのでアンサンブルに似た効果もあります。
正則化以外の過学習への対処は :: データを増やすことです。最も効果的ですが、費用と時間がかかります。
\`\`\`
`,
  },

  {
    id: 'e-dl-5',
    categoryId: 'e-dl',
    title: '誤差逆伝播法',
    goal: '誤差逆伝播法の考え方と、そこで起きる問題を説明できる',
    minutes: 18,
    body: `
# ざっくり言うと

学習の 3 手順の 3 番目、**「外れが減る向きにパラメータをずらす」**に入ります。

ここで、大きな問題があります。**ネットワークには何百万個ものパラメータがあります。そのうちのどれを、どちらへ、どれだけ動かせばいいのでしょうか。**

しかも、誤差が分かるのは**出力側だけ**です。入力に近い層のパラメータが、最終的な誤差にどれだけ責任があるのか——**それを割り当てる方法が必要**でした。

この問題を**信用割当問題**といい、**それを解いたのが誤差逆伝播法**です。

# 信用割当問題：誰のせいで外れたのか

野球チームが負けたとします。**「誰のせいか」を決めるのは簡単ではありません。**

打てなかった打者か、失点した投手か、守備のミスか。**それぞれの責任の重さを割り当てなければ、誰をどう改善すべきか決まりません。**

ニューラルネットワークでも同じです。**出力の誤差に対して、各層の各パラメータがどれだけ責任を持つのか。** これを決める問題が**信用割当問題**です。

> **試験のポイント** **信用割当問題は「出力の誤差に対して、どのパラメータがどれだけ寄与したかを割り当てる」問題**です。**誤差逆伝播法がこれを解きました。**

# 誤差逆伝播法：誤差を逆向きにさかのぼらせる

**誤差逆伝播法**（バックプロパゲーション）の考え方は、**「誤差を出力側から入力側へ、逆向きに伝えていく」**ことです。

\`\`\`diagram:compare
title: 順伝播と逆伝播
actors: 順伝播 | 逆伝播
入力側から出力側へ :: データを流して予測を出す
出力側から入力側へ :: 誤差を流して直し方を求める
何と答えるかを決める :: 予測の計算
どのパラメータをどう直すかを決める :: 勾配の計算
\`\`\`

**まずデータを入力側から出力側へ流して予測を出し（順伝播）、次に誤差を出力側から入力側へ流して、各パラメータの直し方を求める（逆伝播）。** この 2 往復を繰り返すのが学習です。

# 連鎖律：数学の土台

誤差逆伝播法を支えている数学が**連鎖律**です。

難しく聞こえますが、**言っていることは日常的**です。**「A が B に影響し、B が C に影響するなら、A が C に与える影響は、その 2 つを掛け合わせたもの」。**

歯車を思い浮かべてください。歯車 A が歯車 B を 2 倍の速さで回し、B が C を 3 倍の速さで回すなら、**A は C を 6 倍の速さで回します。** 2 × 3 = 6。**これが連鎖律です。**

ニューラルネットワークでも同じことをします。**入力側のパラメータが最終的な誤差に与える影響は、途中の層の影響を掛け合わせることで求まります。**

**そして、この「掛け合わせる」という性質が、次に見る 2 つの問題を生みます。**

> **試験のポイント** **連鎖律は合成関数の微分の規則**で、**誤差逆伝播法の数学的な土台**です。「A→B→C の影響は掛け算でつながる」と押さえてください。

# 掛け算が生む 2 つの問題

連鎖律が「掛け算」である以上、**層が深いほど、たくさんの数を掛け合わせる**ことになります。ここで 2 つの問題が起きます。

\`\`\`diagram:compare
title: 勾配消失問題と勾配爆発問題
actors: 勾配消失問題 | 勾配爆発問題
1 未満の値を掛け続ける :: 0.25 を 10 回掛けると、ほぼ 0
1 より大きい値を掛け続ける :: 2 を 10 回掛けると、1024
勾配がほぼ 0 になる :: 入力側の層が学習しない
勾配が異常に大きくなる :: パラメータが吹き飛んで学習が壊れる
シグモイドが原因になりやすい :: 微分の最大値が 0.25
大きい重みが原因になりやすい :: 1 より大きい値が掛かり続ける
対処は ReLU、スキップ結合など :: 掛け算を 1 に近づける
対処は勾配クリッピング :: 大きさに上限をかぶせる
\`\`\`

**勾配消失問題**は[活性化関数の節](textbook/e-dl-2)で見たとおりです。**1 未満の値を層の数だけ掛け合わせると、入力側では 0 になります。**

**勾配爆発問題**はその逆で、**1 より大きい値を掛け続けると、値が発散します。** パラメータが一気に極端な値へ飛び、学習が壊れます。

**勾配爆発への対処**としてよく使われるのが**勾配クリッピング**です。**勾配の大きさに上限を設け、それを超えたら切り詰めます。** 「1 歩の幅に上限をつける」ようなものです。

> **よくある勘違い** **勾配消失と勾配爆発は、原因が同じで向きが逆**です。どちらも「連鎖律で値を掛け合わせる」ことから生じます。**別々の原因による別々の問題ではありません。**

**この 2 つの問題は、要素技術の章で扱う RNN（回帰結合層）で特に深刻になります。** RNN は同じ重みを時間方向に何度も掛けるためです。

# 誤差逆伝播法が「発見」だった理由

いまとなっては当たり前に見えますが、**この方法が広まるまでニューラルネットワークは深くできませんでした。**

信用割当問題が解けなければ、**隠れ層のパラメータをどう直せばいいか分からない**からです。[LeNet（1998 年）](textbook/b-trend-5)が「誤差逆伝播法で学習する CNN」として重要なのは、この方法を使っていたためです。

**そして皮肉なことに、誤差逆伝播法そのものが勾配消失問題を生みました。** 深くできるようになったのに、深くすると学習しない。**この矛盾を解いたのが ReLU であり、正規化層であり、スキップ結合**です。それらは要素技術の章で扱います。

# この節のまとめ

- **信用割当問題**は「出力の誤差に対して、どのパラメータがどれだけ寄与したかを割り当てる」問題
- **誤差逆伝播法**は、**誤差を出力側から入力側へ逆向きに伝えて**、各パラメータの直し方を求める方法
- **順伝播は入力から出力へ予測を出す、逆伝播は出力から入力へ誤差を流す**
- **連鎖律**は合成関数の微分の規則で、誤差逆伝播法の数学的な土台。**影響は掛け算でつながる**
- **掛け算であるがゆえに、勾配消失問題（1 未満を掛け続けて 0 になる）と勾配爆発問題（1 より大きい値を掛け続けて発散する）が起きる**
- **この 2 つは原因が同じで向きが逆**。別々の原因ではない
- **勾配爆発への対処は勾配クリッピング**（勾配の大きさに上限を設ける）

\`\`\`quiz
信用割当問題とは何ですか :: 出力の誤差に対して、どのパラメータがどれだけ寄与したかを割り当てる問題です。誤差逆伝播法がこれを解きました。
誤差逆伝播法はどういう方法ですか :: 誤差を出力側から入力側へ逆向きに伝えて、各パラメータをどう直すかを求める方法です。
順伝播と逆伝播はそれぞれ何をしていますか :: 順伝播は入力から出力へデータを流して予測を出し、逆伝播は出力から入力へ誤差を流して直し方を求めます。
連鎖律を日常の言葉で言うと :: A が B に影響し B が C に影響するなら、A が C に与える影響はその 2 つを掛け合わせたものになる、という規則です。
勾配消失問題と勾配爆発問題の関係は :: 原因は同じで向きが逆です。どちらも連鎖律で値を掛け合わせることから生じ、1 未満なら消失、1 より大きいなら爆発します。
勾配爆発問題への代表的な対処は :: 勾配クリッピングです。勾配の大きさに上限を設け、それを超えたら切り詰めます。
勾配消失問題が起きやすい活性化関数は :: シグモイド関数です。微分の最大値が 0.25 しかないため、掛け合わせるとすぐ 0 に近づきます。
勾配消失や勾配爆発が特に深刻になるのはどんなネットワークですか :: RNN のような再帰的なネットワークです。同じ重みを時間方向に何度も掛けるためです。
\`\`\`
`,
  },

  {
    id: 'e-dl-6',
    categoryId: 'e-dl',
    title: '最適化手法',
    goal: '勾配降下法の問題と、それを解決する手法の系譜を説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

前の節で、**各パラメータをどちらへ動かせばいいか（勾配）**は求まりました。残るのは、**どれだけ動かすか、どういう順番で動かすか**です。

これを扱うのが**最適化手法**です。

情景で持っておいてください。**霧の中の山で、いちばん低い場所を目指して降りる**——これが学習です。全体は見えませんが、**足元の傾きなら分かります。**

- どちらへ進むか → **勾配**（前の節）
- **1 歩の幅をどうするか** → **学習率**（この節）
- **どう工夫すれば速く確実に降りられるか** → **最適化手法**（この節）

# 勾配降下法：坂を下る

**勾配降下法**の式は、[数式の読み方](textbook/i-3)の最後で読み下したものです。

\`\`\`math
\\theta \\leftarrow \\theta - \\eta \\nabla L
\`\`\`

**「パラメータ $\\theta$ を、誤差 $L$ が減る向きへ、学習率 $\\eta$ のぶんだけずらす」。** それだけです。

**学習率 $\\eta$ が 1 歩の幅**にあたります。ここに、最も基本的な失敗が 2 つあります。

\`\`\`diagram:compare
title: 学習率の大きすぎ・小さすぎ
actors: 学習率が大きすぎる | 学習率が小さすぎる
谷を飛び越えてしまう :: 反対側の斜面へ行ってしまう
なかなか進まない :: 少しずつしか動かない
行ったり来たりして収束しない :: 誤差が減らない、または発散する
学習に時間がかかりすぎる :: いつまでも底に着かない
\`\`\`

> **試験のポイント** **学習率が大きすぎると発散や振動、小さすぎると学習が進まない。** この 2 つの失敗は必ず問われます。

# 何件ずつ見て更新するか

**「1 回の更新に、何件のデータを使うか」**にも選択肢があります。

| 名前 | 1 回の更新に使うデータ | 特徴 |
| --- | --- | --- |
| **バッチ学習** | **全データ** | 安定するが、1 回の計算が重い。局所解にはまりやすい |
| **オンライン学習** | **1 件ずつ** | 計算は軽いが、更新が不安定に振れる |
| **ミニバッチ学習** | **数十〜数百件** | 両者の中間。**実際にはこれが使われる** |

**確率的勾配降下法（SGD）**は、**全データではなく、ランダムに選んだデータで勾配を計算して更新する**方法です。「確率的」という名前は、**毎回ランダムにデータを選ぶ**ところから来ています。

**用語の使われ方に幅があるので注意してください。** 本来の確率的勾配降下法は**1 件ずつランダムに選んで更新する（＝オンライン学習）**ものを指します。ところが実務では、**ミニバッチで更新するものも SGD と呼ぶ**のが普通です。**試験では、まず「1 件ずつ」という本来の意味を押さえてください。**

**SGD には思わぬ利点があります。** 毎回違うデータを使うため、勾配に**ゆらぎ**が生じます。このゆらぎのおかげで、**浅い谷にはまっても抜け出せる**ことがあります。

## エポックとイテレーション

**混同しやすい 2 語**です。

- **エポック**……**訓練データを端から端まで 1 周**させること
- **イテレーション**……**パラメータを 1 回更新**すること

数値で確かめます。**訓練データ 10000 件、バッチサイズ 100** なら、

- 1 エポック＝ 10000 ÷ 100 ＝ **100 イテレーション**
- 10 エポックなら **1000 イテレーション**

> **試験のポイント** **エポックはデータを 1 周、イテレーションは更新 1 回。** バッチサイズが分かれば、1 エポックあたりのイテレーション数は割り算で出ます。

# 底にたどり着けない 3 つの理由

坂を下るだけでは、**いちばん低い場所にたどり着けるとは限りません。**

\`\`\`diagram:tree
title: 学習を妨げる地形
局所最適解 :: 周りより低いが、全体で最も低くはない谷
  全体で最も低い場所が大域最適解 :: 本当に目指したいところ
鞍点 :: ある方向から見ると谷、別の方向から見ると山になっている点
  勾配がほぼ 0 になるので止まってしまう :: 高次元では局所最適解より深刻
プラトー :: 傾きがほとんどない平らな場所
  進むべき方向が分からず停滞する :: なかなか抜けられない
\`\`\`

**局所最適解**は「周りより低いが、本当の底ではない谷」です。そこに落ちると、**足元の傾きだけでは抜け出せません。**

**鞍点**は、**馬の鞍のような形**をした点です。前後の方向から見ると谷ですが、左右の方向から見ると山。**傾きがほぼ 0 になるので、そこで止まってしまいます。**

> **よくある勘違い** 「ディープラーニングの学習で最も問題になるのは局所最適解だ」というのは、いまでは正確ではありません。**パラメータが非常に多い（高次元の）空間では、局所最適解より鞍点のほうが圧倒的に多く、問題になりやすい**と考えられています。

# 手法の系譜：何を改善してきたか

最適化手法には名前がたくさんありますが、**「何の不満から出てきたか」**でつなぐと 1 本の線になります。

\`\`\`diagram:flow
title: 最適化手法の系譜
勾配降下法 :: 基本。学習率が固定で、鞍点や振動に弱い
モーメンタム :: 前回の進行方向を慣性として引き継ぐ。振動を抑え、平らな場所を抜けやすい
AdaGrad :: パラメータごとに学習率を自動調整。ただし学習率が下がりすぎる
RMSprop :: 古い勾配を忘れるようにして、学習率が下がりすぎる問題を改善
Adam :: モーメンタムと RMSprop を組み合わせたもの。いま最もよく使われる
\`\`\`

## モーメンタム

**前回の進行方向を「慣性」として引き継ぐ**工夫です。

坂を転がるボールを思い浮かべてください。**ボールは前の勢いを持っているので、小さなくぼみは通り抜けます。** 同じように、モーメンタムは**振動を抑え、平らな場所を抜けやすく**します。

## AdaGrad

**パラメータごとに学習率を自動で変える**手法です。

**よく更新されるパラメータは学習率を小さく、あまり更新されないパラメータは学習率を大きく**します。「もう十分動かしたものは慎重に、まだ動かしていないものは大胆に」という考え方です。

**ただし弱点があります。** 更新が進むにつれて**学習率が下がり続け、最後にはほとんど動かなくなります。**

## RMSprop と AdaDelta

**AdaGrad の「学習率が下がりすぎる」問題を改善した**手法です。

**古い勾配の情報を少しずつ忘れる**ようにして、学習率が下がり続けるのを防ぎます。**AdaDelta も同じ問題への別の解**です。

## Adam

**モーメンタムと RMSprop を組み合わせた**手法です。「慣性」と「パラメータごとの学習率調整」の両方を持ちます。

**いま最もよく使われる最適化手法**で、迷ったら Adam を選ぶのが定石とされています。

## AdaBound と AMSBound

**Adam にも弱点があり、最終的な性能では SGD に劣ることがある**と指摘されました。

そこで、**Adam の学習率に上限と下限を設け、学習が進むにつれて SGD に近づけていく**手法が **AdaBound** です。**AMSBound** はその変種にあたります。

> **試験のポイント** **モーメンタムは慣性、AdaGrad はパラメータごとの学習率調整（ただし下がりすぎる）、RMSprop はその改善、Adam はモーメンタムと RMSprop の組み合わせ。** この系譜を順に言えるようにしてください。

# ハイパーパラメータをどう決めるか

学習率、バッチサイズ、層の数——これらは**学習では決まりません。人が決める設定**で、**ハイパーパラメータ**といいます。

決め方には、機械に探させる方法があります。

\`\`\`diagram:compare
title: グリッドサーチとランダムサーチ
actors: グリッドサーチ | ランダムサーチ
候補を格子状に並べて全部試す :: 学習率 0.1, 0.01, 0.001 の 3 通り × バッチ 32, 64 の 2 通り
候補をランダムに選んで試す :: 決められた回数だけ、でたらめに選ぶ
組み合わせが増えると爆発する :: 項目が増えるほど試行回数が掛け算で増える
試行回数を先に決められる :: 時間の予算に合わせられる
効かない項目にも同じ手間をかける :: 無駄が多い
効く項目を細かく探せることがある :: 同じ回数ならこちらが有利なことが多い
\`\`\`

**意外なことに、同じ試行回数ならランダムサーチのほうが良い結果になることが多い**と報告されています。効かないハイパーパラメータに手間をかけずに済むためです。

# 学習をいつ止めるか

**早期終了**は、**検証データでの誤差が悪化し始めたら、そこで学習を止める**方法です。

\`\`\`diagram:flow
title: 早期終了の考え方
学習を進める :: 訓練誤差も検証誤差も下がっていく
ある時点から :: 訓練誤差は下がり続けるが、検証誤差が上がり始める
それが過学習の始まり :: 訓練データに合わせ込み始めた合図
そこで止める :: 検証誤差が最も低かった時点のモデルを採用する
\`\`\`

**単純ですが強力**で、正則化の一種としても扱われます。

# 2 つの「そうとは限らない」

最後に、直感に反する 2 つの事実です。

## 二重降下現象

**「モデルを大きくすると、いったん性能が悪化し、さらに大きくすると再び良くなる」**という現象が報告されています。これを**二重降下現象**といいます。

従来の常識では、**モデルを複雑にしすぎれば過学習して悪化する一方**のはずでした。ところが**さらに大きくすると、また良くなる**。**近年になって注目された現象**で、シラバスにも追加されました。

## ノーフリーランチの定理

**「あらゆる問題に対して万能な手法は存在しない」**という主張です。「ただ飯はない」という言い回しから来ています。

**ある問題でうまくいく手法は、別の問題ではうまくいかない。** だから、**問題に応じて手法を選ばなければならない**——という結論になります。

> **試験のポイント** **ノーフリーランチの定理は「万能な手法は存在しない」。** 「Adam はあらゆる問題で最良である」のような選択肢は、この定理に反するので誤りです。

# この節のまとめ

- **勾配降下法**は「誤差が減る向きへ、学習率のぶんだけパラメータをずらす」
- **学習率が大きすぎると発散や振動、小さすぎると学習が進まない**
- **バッチ学習は全データ、オンライン学習は 1 件ずつ、ミニバッチ学習はその中間。** 実際にはミニバッチが使われる
- **確率的勾配降下法（SGD）**は、ランダムに選んだデータで更新する。**本来は 1 件ずつ（オンライン学習）**を指すが、実務ではミニバッチのものも SGD と呼ぶ。**ゆらぎのおかげで浅い谷を抜け出せる**ことがある
- **エポックはデータを 1 周、イテレーションは更新 1 回**
- 学習を妨げる地形は**局所最適解・鞍点・プラトー**。**高次元では鞍点のほうが問題になりやすい**
- **モーメンタムは慣性、AdaGrad はパラメータごとの学習率調整（下がりすぎる欠点）、RMSprop はその改善、Adam はモーメンタムと RMSprop の組み合わせ**
- **AdaBound・AMSBound** は Adam の学習率に上下限を設けて SGD に近づける
- **ハイパーパラメータは人が決める設定。** 探索には**グリッドサーチ**と**ランダムサーチ**があり、**同じ試行回数ならランダムサーチが有利なことが多い**
- **早期終了**は、検証誤差が悪化し始めたら学習を止める方法
- **二重降下現象**は、モデルを大きくするといったん悪化してから再び良くなる現象
- **ノーフリーランチの定理**は「あらゆる問題に万能な手法は存在しない」

\`\`\`quiz
勾配降下法の式は何を言っていますか :: パラメータを、誤差が減る向きへ、学習率のぶんだけずらす、という意味です。
学習率が大きすぎるとどうなりますか :: 谷を飛び越えてしまい、行ったり来たりして収束しなかったり、発散したりします。
バッチ学習・オンライン学習・ミニバッチ学習の違いは :: 1 回の更新に使うデータが、それぞれ全データ・1 件ずつ・数十から数百件です。
確率的勾配降下法の「確率的」とは何を指しますか :: 毎回ランダムにデータを選んで勾配を計算することです。本来は 1 件ずつを指しますが、実務ではミニバッチのものも SGD と呼びます。
訓練データ 10000 件、バッチサイズ 100 のとき、1 エポックは何イテレーションですか :: 100 イテレーションです。10000 を 100 で割った数になります。
鞍点とはどんな点ですか :: ある方向から見ると谷、別の方向から見ると山になっている点です。勾配がほぼ 0 になるので学習が止まります。
高次元の空間で問題になりやすいのは局所最適解と鞍点のどちらですか :: 鞍点です。パラメータが非常に多い空間では、局所最適解より鞍点のほうが圧倒的に多くなります。
モーメンタムはどんな工夫ですか :: 前回の進行方向を慣性として引き継ぐ工夫です。振動を抑え、平らな場所を抜けやすくします。
AdaGrad の利点と弱点は :: パラメータごとに学習率を自動調整できる一方、更新が進むと学習率が下がり続けてほとんど動かなくなります。
Adam は何と何を組み合わせた手法ですか :: モーメンタムと RMSprop を組み合わせた手法です。
グリッドサーチとランダムサーチの違いは :: グリッドサーチは候補を格子状に並べて全部試し、ランダムサーチは決められた回数だけでたらめに選んで試します。
早期終了とはどんな方法ですか :: 検証データでの誤差が悪化し始めたら、そこで学習を止める方法です。過学習を防ぐ効果があります。
二重降下現象とは何ですか :: モデルを大きくしていくと、いったん性能が悪化した後、さらに大きくすると再び良くなる現象です。
ノーフリーランチの定理とは何ですか :: あらゆる問題に対して万能な手法は存在しない、という主張です。問題に応じて手法を選ぶ必要があります。
\`\`\`
`,
  },
];
