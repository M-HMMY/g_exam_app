import type { TextbookSection } from '../../types';

/** 技術分野：要素技術（2）系列・生成・データ拡張（categoryId: 'g-seq'）の節。 */
export const gSeq: TextbookSection[] = [
  {
    id: 'g-seq-1',
    categoryId: 'g-seq',
    title: '回帰結合層と RNN',
    goal: 'RNN の仕組みと限界を説明でき、LSTM がそれをどう解いたか言える',
    minutes: 25,
    body: `
# ざっくり言うと

前の 4 節では**画像**を扱いました。ここからは**順番のあるデータ**を扱います。

文章、音声、株価、センサーの記録——これらは**順番に意味があるデータ**で、**系列データ**と呼ばれます。そのうち**株価やセンサーの記録のように、時刻の順に並ぶもの**を**時系列データ**といいます。**順番を崩すと意味が変わります。** 「私は犬が好き」と「犬は私が好き」では、同じ語を使っていても意味が違います。

**画像用の部品は、そのままでは向きません。** 畳み込み層は「近くの画素どうし」を見るので、**文章のように遠く離れた語どうしが関係する**データを捉えるのが苦手です（**まったく使えないわけではなく**、工夫すれば系列にも使えます）。

そこで生まれたのが**回帰結合層**、それを使ったネットワークが **RNN（リカレントニューラルネットワーク）**です。

そして、**この節と次の節は 1 本の物語です。**

\`\`\`diagram:flow
title: この節と次の節でたどる流れ
RNN :: 前から順に読む。遠い過去を忘れる
LSTM :: ゲートで記憶を守る。ただし順番に読むのは変わらない
Seq2Seq（系列を受け取って系列を出すしくみ） :: 入力を 1 本のベクトルに詰めて渡す。詰めきれない
Attention :: どこに注目するかを重みで決める。遠い依存の問題が解ける
Transformer :: Attention だけで作る。順番に読む制約から解放される
\`\`\`

**「前から順に読む」という制約を、どうやって外していったか。** この一本で読んでください。

# RNN：前の結果を、次に持ち越す

**回帰結合層**の発想は単純です。**「前の時刻の計算結果を、次の時刻の入力に混ぜる」。**

\`\`\`diagram:seq
title: RNN が文章を読む様子
actors: 入力 | 隠れ状態
入力 -> 隠れ状態 :: 1 語目「私」を読む。隠れ状態ができる
隠れ状態 -> 隠れ状態 :: それを次へ持ち越す
入力 -> 隠れ状態 :: 2 語目「は」を、持ち越した状態と一緒に読む
隠れ状態 -> 隠れ状態 :: また次へ持ち越す
入力 -> 隠れ状態 :: 3 語目「犬」を、これまでの状態と一緒に読む
\`\`\`

この持ち越される値を**隠れ状態**といいます。**「ここまで読んだ内容の要約」**だと思ってください。

**人が文章を読むときと同じ**です。3 行目を読むとき、1〜2 行目の内容を頭に置いたまま読んでいます。**その「頭に置いたまま」を実現するのが回帰結合層**です。

## 2 つの原型

**戻し方が 2 通り**あり、**この違いが試験に出ます。**

\`\`\`diagram:compare
title: エルマンネットワークとジョルダンネットワーク
actors: エルマンネットワーク | ジョルダンネットワーク
隠れ層の出力を戻す :: 中間の状態を次へ持ち越す
出力層の出力を戻す :: 最終的な答えを次へ持ち越す
文脈をそのまま引き継げる :: 内部の要約が伝わる
出した答えが次に影響する :: 答えの系列を作るのに向く
\`\`\`

> **試験のポイント** **エルマンネットワークは隠れ層の出力を戻し、ジョルダンネットワークは出力層の出力を戻す。** 名前と戻す場所の対応が問われます。

## 学習は「時間方向に展開して」行う

RNN の学習には、**BPTT（Backpropagation Through Time）**という方法を使います。名前のとおり、**時間をさかのぼって誤差を伝える**ものです。

やっていることは、**時間方向に展開して、とても深いネットワークとして扱う**ことです。100 語の文章を読ませるなら、**100 層のネットワークとして誤差逆伝播する**のと同じになります。

**そして、ここに致命的な問題が現れます。**

# RNN の限界：遠い過去を忘れる

[誤差逆伝播法の節](textbook/e-dl-5)で見たとおり、**深いネットワークでは勾配消失と勾配爆発が起きます。**

RNN では、これが**特に深刻**です。理由は、**同じ重みを何度も掛ける**からです。**同じ値を繰り返し掛けるので、1 未満なら一気に 0 へ、1 より大きければ一気に発散します。**

**対処は、消える側と発散する側で違います。**

- **勾配爆発**（発散する側）……**勾配クリッピング**（[誤差逆伝播法](textbook/e-dl-5)で見た、勾配が大きくなりすぎたら上限で切る方法）で抑えます
- **勾配消失**（消える側）……こちらは切っても直りません。**構造そのものを変える**必要があります。**それが次に見る LSTM です**

\`\`\`diagram:compare
title: RNN で勾配の問題が深刻になる理由
actors: 通常の深いネットワーク | RNN
層ごとに別の重みを学習する :: 掛かる値がばらつく
同じ重みを何度も掛ける :: 1 未満なら一気に 0、1 より大なら一気に発散
重みが層ごとに違う :: 大きくする層と小さくする層が打ち消し合う
100 語なら 100 回掛かる :: 長い文章では避けられない
\`\`\`

結果として何が起きるか。**遠い過去の情報が、勾配とともに消えます。**

たとえば「**フランス**で生まれ育ち、大学では物理を学び、卒業後は各地を転々としたが、彼は流暢な**フランス語**を話した」という文。「フランス語」を予測するには、**ずっと前の「フランス」**が必要です。**ところが RNN は、そこまで届きません。**

これを**長期依存の問題**といいます。

# LSTM：ゲートで記憶を守る

**LSTM（Long Short-Term Memory）**は、**1997 年にホッフライターとシュミットフーバーが提案**した、この問題への答えです。名前は「**長い**短期記憶」という意味で、**短期記憶を長く保つ**という狙いを表しています。

発想はこうです。**「そのまま持ち越す専用の通り道を作り、そこに出し入れする門（ゲート）を付ける」。**

\`\`\`diagram:tree
title: LSTM の 3 つのゲート
記憶セル（CEC） :: 情報をそのまま持ち越す通り道。ここが本体
  入力ゲート :: 新しい情報を、どれだけ記憶に書き込むか
  忘却ゲート :: いまの記憶を、どれだけ残す（忘れる）か
  出力ゲート :: 記憶のうち、どれだけを次へ出力するか
\`\`\`

この**ゲートのしくみ**を**ゲート機構**といいます。**ゲートの開き具合も学習で決まります。** 「この語は重要だから記憶に入れる」「話題が変わったから前の記憶は忘れる」——**それをデータから学ぶ**わけです。

この**記憶セル**は、**CEC（Constant Error Carousel＝誤差を一定に保つ回転木馬）**とも呼ばれます。**誤差（勾配）を小さくせずにぐるぐる回して運び続ける**、という意味の名前です。

**記憶セルという「そのまま通す道」があるおかげで、勾配が消えずに遠くまで届きます。** 発想は、[スキップ結合と ResNet](textbook/f-layer-4)と同じです。**「まっすぐ通る道を作る」。**

> **試験のポイント** **LSTM のゲートは 3 つ。入力ゲート・忘却ゲート・出力ゲート。** 数と名前をそのまま覚えてください。

## GRU：ゲートを 2 つに減らした

**GRU（Gated Recurrent Unit）**は、**LSTM を簡略化した**ものです。

**ゲートを 2 つ（リセットゲートと更新ゲート）に減らし、記憶セルと隠れ状態を 1 つにまとめました。**

\`\`\`diagram:compare
title: LSTM と GRU
actors: LSTM | GRU
ゲートは 3 つ :: 入力・忘却・出力
ゲートは 2 つ :: リセット・更新
記憶セルと隠れ状態を分けて持つ :: 構造が複雑
1 つにまとめている :: 構造が単純
パラメータが多く、計算が重い :: 学習に時間がかかる
パラメータが少なく、計算が軽い :: 性能は同程度のことが多い
\`\`\`

> **試験のポイント** **LSTM はゲート 3 つ、GRU はゲート 2 つ。GRU のほうが軽い。** この対比が問われます。

# 前からだけでなく、後ろからも読む

**双方向 RNN（Bidirectional RNN）**は、**系列を前からと後ろからの両方向で読む**ものです。

「彼は**銀行**へ行った」の「銀行」の意味は、後ろの語まで読まないと確定しないことがあります。**前後の文脈を両方使えれば、精度は上がります。**

> **よくある勘違い** **双方向 RNN は、リアルタイムに先を予測する用途には使えません。** 後ろから読むには**系列全体がすでに揃っている**必要があるからです。「今後の株価をその場で予測する」ような使い方はできません。翻訳や文章の分類のように、**入力が最初から全部そろっている**場面で使います。

# 教師強制：学習を速くする代わりに

系列を出力させるモデルには、**学習の工夫**があります。

文章を生成するモデルは、**自分が出した語を次の入力にします。** ところが学習の初期には、出す語がでたらめです。**でたらめを次の入力にすると、その先もずっとでたらめになり、学習が進みません。**

そこで、**学習中は「自分の出力」ではなく「正解」を次の入力に使います。** これを**教師強制**（teacher forcing）といいます。

\`\`\`diagram:compare
title: 教師強制の利点と弱点
actors: 利点 | 弱点
学習が速く安定する :: 途中で崩れても引きずらない
学習時と推論時で条件がずれる :: 推論時は正解を使えない
初期からまともに学習が進む :: でたらめの連鎖を防げる
本番で一度間違えると崩れやすい :: 間違いから立て直す訓練をしていない
\`\`\`

> **試験のポイント** **教師強制は、学習時に前ステップの出力ではなく正解を次の入力に使う手法。** 学習は速く安定しますが、**学習時と推論時で条件がずれる**という弱点があります。

# それでも残った制約

LSTM と GRU で、**RNN は長い系列をかなり扱えるようになりました。** 実際、機械翻訳や音声認識で長く主力でした。

**しかし、根本的な制約は残ります。**

1. **前から順にしか処理できない。** 1 語目を処理し終わらないと 2 語目に進めないので、**並列に計算できません。** GPU の力を活かせません
2. **遠い依存はやはり苦手。** ゲートで緩和されただけで、**距離が離れるほど不利**であることは変わりません

**この 2 つを一気に解いたのが、次の節の Attention と Transformer** です。

# この節のまとめ

- **回帰結合層**は「前の時刻の計算結果を、次の時刻の入力に混ぜる」。持ち越される値が**隠れ状態**
- **エルマンネットワークは隠れ層の出力を戻し、ジョルダンネットワークは出力層の出力を戻す**
- 学習は **BPTT**（時間方向に展開して誤差逆伝播する）
- **RNN では勾配消失・勾配爆発が特に深刻。** 理由は**同じ重みを何度も掛ける**こと。結果として**遠い過去を忘れる**（長期依存の問題）
- **勾配爆発には勾配クリッピング、勾配消失には LSTM のゲート機構**——**対処が別**
- **LSTM（1997 年）は記憶セル（CEC）という「そのまま通す道」と 3 つのゲート（入力・忘却・出力）**で、記憶を長く保つ
- **GRU はゲートを 2 つ（リセット・更新）に減らした簡略版。** 軽く、性能は同程度のことが多い
- **双方向 RNN は前後の両方向から読む。** ただし**系列全体が揃っている必要があり、リアルタイム予測には使えない**
- **教師強制**は学習時に正解を次の入力に使う手法。速く安定するが、**学習時と推論時で条件がずれる**
- 残る制約は、**前から順にしか処理できない（並列化できない）**ことと、**遠い依存がやはり苦手**なこと

\`\`\`quiz
回帰結合層の基本的な発想は :: 前の時刻の計算結果を、次の時刻の入力に混ぜることです。持ち越される値を隠れ状態といいます。
エルマンネットワークとジョルダンネットワークの違いは :: エルマンは隠れ層の出力を戻し、ジョルダンは出力層の出力を戻します。
BPTT とは何ですか :: 時間方向に展開して誤差逆伝播する学習方法です。100 語の文章なら 100 層のネットワークのように扱います。
RNN で勾配の問題が特に深刻なのはなぜですか :: 同じ重みを時間方向に何度も掛けるためです。1 未満なら一気に 0、1 より大きければ一気に発散します。
LSTM のゲートを 3 つ挙げてください :: 入力ゲート、忘却ゲート、出力ゲートです。
LSTM の記憶セルは何と呼ばれますか :: CEC（Constant Error Carousel）です。誤差を小さくせずに運び続ける通り道という意味です。
勾配爆発への代表的な対処は :: 勾配クリッピングです。勾配が大きくなりすぎたら上限で切ります。勾配消失のほうは LSTM のような構造の工夫で対処します。
LSTM はいつ、誰が提案しましたか :: 1997 年にホッフライターとシュミットフーバーが提案しました。
LSTM が遠い過去を覚えていられるのはなぜですか :: 記憶セルという情報をそのまま持ち越す通り道があり、勾配が消えずに遠くまで届くからです。
GRU は LSTM とどう違いますか :: ゲートが 2 つ（リセットと更新）に減り、記憶セルと隠れ状態が 1 つにまとまっています。軽く、性能は同程度のことが多いです。
双方向 RNN が使えない場面は :: 系列全体がまだ揃っていない、リアルタイムに先を予測するような場面です。後ろから読むには全体が必要だからです。
教師強制とはどんな手法ですか :: 学習時に、前ステップの自分の出力ではなく正解を次の入力に使う手法です。
教師強制の弱点は :: 学習時と推論時で条件がずれることです。推論時には正解を使えないため、一度間違えると立て直しにくくなります。
LSTM でも残った RNN の制約を 2 つ挙げてください :: 前から順にしか処理できず並列化できないことと、距離が離れるほど依存関係を捉えにくいことです。
\`\`\`
`,
  },

  {
    id: 'g-seq-2',
    categoryId: 'g-seq',
    title: 'Attention と Transformer',
    goal: 'Attention の役割を説明でき、Transformer の位置づけを言える',
    minutes: 25,
    body: `
# ざっくり言うと

**この節は、いまの生成 AI に直結します。** ここを理解しておくと、応用例の章が半分楽になります。

> **重要な注意** **この節の記述は 2026 年 9 月時点のものです。** 生成 AI の分野は変化が速いので、**最新の状況は自分で確かめてください。**

前の節で残った RNN の制約は 2 つでした。**「前から順にしか読めない」**と**「遠い依存が苦手」**。

**この 2 つは、別々に外されました。順番が大事です。**

- **Attention（アテンション）**が、**「遠い依存が苦手」**を外しました。ただし**RNN と組み合わせて使われた**ので、**「前から順にしか読めない」はそのまま残りました**
- **そのあと Transformer が、RNN を捨てて Attention だけで組み立て**、**残る「前から順に」も外しました**

**この 2 段階を 1 つにまとめないでください。** 「Attention が並列化を可能にした」は誤りです。

名前は英語の attention（＝注意）から来ています。**「どこに注目するかを、重みで決める」**という意味です。

**人が長文を読むときと同じ**です。「彼はそれを買った」の「それ」が何を指すか考えるとき、私たちは**前の文の特定の名詞に注意を戻します。** 文章を最初から読み直したりはしません。**その「注意を向ける」を機械にやらせるのが Attention です。**

# その前に：Seq2Seq とボトルネック

Attention が生まれた背景を押さえます。

**Seq2Seq（sequence to sequence）**は、**2014 年に提案された、系列を受け取って系列を出す**モデルです。機械翻訳がまさにこれで、日本語の系列を受け取って英語の系列を出します。

構成は**エンコーダとデコーダの 2 つ**に分かれます。**エンコーダ（encoder＝符号化するもの）**が入力を読み込んで要約し、**デコーダ（decoder＝復号するもの）**がそこから出力を作ります。

\`\`\`diagram:flow
title: Seq2Seq の構成
入力の系列 :: 「私は犬が好きです」
エンコーダ :: 入力を読み込んで、1 本のベクトルに要約する
固定長のベクトル :: ここに入力の全情報が詰め込まれる
デコーダ :: そのベクトルから出力を作る
出力の系列 :: 「I like dogs」
\`\`\`

**問題は、真ん中の「固定長のベクトル」です。**

**入力が 5 語でも 100 語でも、同じ長さのベクトル 1 本に詰め込まなければなりません。** 長い文章では、当然詰め込みきれません。**これをボトルネック（瓶の首）問題**といいます。

**長い文ほど翻訳精度が落ちる**という、はっきりした症状が出ました。

# Attention：全部を取っておいて、必要なものを見る

Attention の答えは単純です。**「1 本に詰め込むのをやめる。入力の各語の情報を全部取っておいて、出力するたびに必要なところを見に行く」。**

\`\`\`diagram:flow
title: Attention の動き
入力の各語の情報を全部保持する :: 1 本に潰さない
出力の 1 語を作るとき :: 「いま、どの入力語が重要か」を計算する
重みを付けて混ぜる :: 重要な語の情報を厚く、そうでない語を薄く
その混ぜたものを使って出力する :: 必要な情報だけを取り出せる
\`\`\`

**この「どの語をどれだけ見るか」の重みは、入力のたびにその場で計算されます。**

翻訳なら、「dogs」を出力するときには入力の「犬」に大きな重みが付きます。**人がそう設計するわけではありません。** ただし、**学習で決まるのは「重みそのもの」ではなく「重みの決め方」**です。**照合のしかたを学習しておき、実際の重みは文が入ってくるたびに計算し直します。**

**これでボトルネックは解消し、遠い語も直接見に行けるようになりました。**

# キー・クエリ・バリュー：検索にたとえる

Attention の中身は、**3 つの役割**で説明されます。**辞書やデータベースの検索**にたとえると分かりやすくなります。

| 名前 | 検索にたとえると | 役割 |
| --- | --- | --- |
| **クエリ（Query）** | **探したい言葉** | いま何を知りたいか |
| **キー（Key）** | **見出し・索引** | 各情報に付いた目印。クエリと照合される |
| **バリュー（Value）** | **中身** | 実際に取り出される情報 |

\`\`\`diagram:flow
title: キー・クエリ・バリューの流れ
クエリを用意する :: 「いま知りたいこと」を表すベクトル
すべてのキーと照合する :: どれだけ合うかを数値にする
合い具合を重みにする :: よく合うキーには大きな重み
重みを付けてバリューを混ぜる :: 必要な中身が濃く出てくる
\`\`\`

**図書館で本を探すのと同じ**です。**探したい主題（クエリ）**を、**背表紙のタイトル（キー）**と照らし合わせ、**合った本の中身（バリュー）**を読む。

> **試験のポイント** **クエリは「知りたいこと」、キーは「照合される目印」、バリューは「取り出される中身」。** この 3 つの役割が問われます。

# 2 種類の Attention

**この区別が最も問われます。**

\`\`\`diagram:compare
title: Self-Attention と Source-Target Attention
actors: Self-Attention | Source-Target Attention
同じ系列の中で注目先を決める :: 入力文の中で、語どうしの関係を見る
別の系列のあいだで注目先を決める :: 入力側と出力側をつなぐ
文中の「それ」が何を指すか分かる :: 同じ文の中の語を参照する
翻訳で対応する語を見つける :: 日本語の語と英語の語を対応させる
入力だけ、出力だけでも使える :: 1 つの系列で完結する
2 つの系列が必要 :: 入力と出力がそろって初めて使える
\`\`\`

- **Self-Attention（自己注意）**……**同じ系列の中**で、どの語がどの語に注目するかを計算する。「彼は犬を飼っている。**それ**はよく吠える」の「それ」が「犬」を指す、といった関係を捉えられます
- **Source-Target Attention**（**Encoder-Decoder Attention** ともいいます）……**入力側（Source）と出力側（Target）という別の系列のあいだ**で注目先を決める。翻訳で対応する語を見つけるのがこれです。**エンコーダとデコーダをつなぐ**位置にあるので、この別名で呼ばれます

**Self-Attention が革命的でした。** RNN なしに、**1 文の中の語どうしの関係を、距離に関係なく直接**捉えられるからです。

# Transformer：Attention だけで作る

**2017 年、「Attention Is All You Need」という論文**が発表されました。題は**「Attention さえあればいい」**という意味です。

そこで提案された **Transformer**（transform＝変換する。**系列を別の系列に変換するもの**、という意味の名前です）は、**RNN も畳み込みも使わず、Attention だけで系列を扱います。**

**構造は、Seq2Seq と同じくエンコーダとデコーダを重ねた形**です。**そして、片方だけを取り出して使うこともできます。** **エンコーダ側だけを使えば「文を読んで理解する」モデル**に、**デコーダ側だけを使えば「前から順に文を作る」モデル**になります。**応用例の章で出てくるモデルは、この「どちら側を使ったか」で分かれます。**

\`\`\`diagram:compare
title: RNN ベースのモデルと Transformer
actors: RNN ベース | Transformer
前から順に処理する :: 1 語ずつしか進めない
学習時は全語を同時に処理できる :: 並列に計算できる
GPU の力を活かせない :: 順番待ちが発生する
GPU の力を最大限使える :: 学習が桁違いに速い
遠い語ほど不利 :: 何度も伝言を経由する
距離に関係なく直接見る :: どの語も 1 回で参照できる
順番の情報は自然に持っている :: 順に読むから
順番の情報が失われる :: 対策が必要になる
\`\`\`

**最後の行が重要です。** 全語を同時に処理するということは、**語の順番という情報が消える**ということです。「犬が人を噛んだ」と「人が犬を噛んだ」が区別できなくなります。

そこで **Transformer は、各語に「何番目か」を表す情報を足しておきます。** これを**位置エンコーディング**（positional encoding）といいます。

> **試験のポイント** **Transformer は RNN も畳み込みも使わず Attention だけで系列を扱う。** **学習時は**全語を同時に処理できるので**並列化でき、学習が速い**。ただし**順番の情報が失われるので、位置エンコーディングで補う。** この 3 点が問われます。

> **よくある勘違い** **「Transformer は生成も並列にできる」わけではありません。** **並列に処理できるのは学習のとき**で、**文章を生成するときは、デコーダが前から 1 語ずつ**作っていきます。**すでに全文がそろっている学習時と、これから作る生成時では、事情が違います。**

## Multi-Head Attention

Transformer のもう 1 つの工夫が **Multi-Head Attention** です。

**Attention を 1 回だけでなく、複数並列に行います。** それぞれの「頭（head）」が**別の観点で注目先を決めます。**

文法的な係り受けに注目する頭、意味の近さに注目する頭、代名詞の指し先に注目する頭——**複数の観点を同時に得られる**わけです。

**なぜ観点が分かれるのか。** 同じ計算を並べただけなら、同じ結果になりそうなものです。**頭ごとに、クエリ・キー・バリューを作る変換を別々に学習するから**です。**入り口が違えば、注目する先も違ってきます。**

> **よくある勘違い** **Multi-Head Attention は「Attention を何回も繰り返す」のではありません。** **複数の Attention を並列に行い、その結果をまとめる**ものです。

# なぜこれが決定的だったのか

Transformer の意義は、**精度が上がったこと以上に、「大きくできるようになった」こと**にあります。

**並列に計算できるので、GPU を大量に並べれば、モデルとデータを大きくしていけます。** この性質が、**大規模言語モデル（LLM）を可能にしました。**

**2026 年時点では、主要な生成 AI のほとんどが Transformer を土台にしています。** 文章だけでなく、画像（Vision Transformer）にも広がっています。

**ただし、弱点もあります。** **すべての語の組み合わせを照合する**ので、**系列が長くなると計算量が急に増えます**（長さの 2 乗に比例して増えます）。**長い文書をそのまま扱うのが苦手**で、ここを改善する研究が続いています。

なお、**Transformer にもスキップ結合とレイヤー正規化が組み込まれています。** [スキップ結合と ResNet](textbook/f-layer-4)、[プーリング層と正規化層](textbook/f-layer-3)で見た部品が、ここでも使われています。

# この節のまとめ

- **Seq2Seq** は系列を受け取って系列を出すモデル。**エンコーダ**が入力を要約し、**デコーダ**が出力を作る
- Seq2Seq の問題は**ボトルネック**。入力の長さによらず**固定長のベクトル 1 本**に詰め込むため、長文で精度が落ちた
- **Attention は「入力の各語の情報を全部取っておき、出力するたびに必要なところを重み付きで見る」。** **Attention が外したのは「遠い依存が苦手」のほうで、「前から順にしか読めない」を外したのは Transformer**
- **クエリは知りたいこと、キーは照合される目印、バリューは取り出される中身**
- **Self-Attention は同じ系列の中で、Source-Target Attention は入力側と出力側という別の系列のあいだ**で注目先を決める
- **Transformer（2017 年「Attention Is All You Need」）は RNN も畳み込みも使わず、Attention だけで系列を扱う。** **エンコーダとデコーダからなり、片方だけを取り出して使うこともできる**
- 全語を同時に処理できるので**並列化でき、学習が速い**。距離に関係なく直接参照できる
- ただし**順番の情報が失われる**ので、**位置エンコーディング**で補う
- **Multi-Head Attention は、Attention を複数並列に行って別々の観点を同時に得る**。繰り返しではない
- Transformer が**大規模言語モデルを可能にした**。**2026 年時点で、主要な生成 AI のほとんどの土台**
- ただし**系列が長くなると計算量が長さの 2 乗で増える**という弱点がある

\`\`\`quiz
Seq2Seq のボトルネック問題とは何ですか :: 入力の長さによらず固定長のベクトル 1 本に情報を詰め込むため、長い文章では詰め込みきれず精度が落ちる問題です。
Attention はボトルネック問題をどう解決しましたか :: 1 本に詰め込むのをやめ、入力の各語の情報を全部保持しておいて、出力のたびに必要なところを重み付きで見るようにしました。
クエリ・キー・バリューはそれぞれ何にあたりますか :: クエリは知りたいこと、キーは照合される目印、バリューは取り出される中身です。
Self-Attention と Source-Target Attention の違いは :: Self-Attention は同じ系列の中で注目先を決め、Source-Target Attention は入力側と出力側という別の系列のあいだで決めます。
Transformer は何を使わずに系列を扱いますか :: RNN も畳み込みも使わず、Attention だけで扱います。
Transformer の学習が速いのはなぜですか :: 学習時は全語を同時に処理できるため並列に計算でき、GPU の力を最大限に使えるからです。
Attention と Transformer は、RNN の 2 つの制約をそれぞれどちらが外しましたか :: Attention が「遠い依存が苦手」を外し、Transformer が「前から順にしか読めない」を外しました。
Source-Target Attention の別名は :: Encoder-Decoder Attention です。エンコーダとデコーダをつなぐ位置にあることから呼ばれます。
Transformer はどんな構造をしていますか :: エンコーダとデコーダを重ねた構造です。エンコーダ側だけ、デコーダ側だけを取り出して使うこともできます。
Transformer で文章を生成するときも並列に処理できますか :: できません。並列に処理できるのは学習時で、生成時はデコーダが前から 1 語ずつ作ります。
Transformer の弱点は :: すべての語の組を照合するため、系列が長くなると計算量が長さの 2 乗で増えることです。
Transformer で位置エンコーディングが必要なのはなぜですか :: 全語を同時に処理すると語の順番という情報が失われてしまうため、各語に何番目かを表す情報を足しています。
Multi-Head Attention とは何ですか :: Attention を複数並列に行い、それぞれ別の観点で注目先を決めて結果をまとめる仕組みです。繰り返しではありません。
Transformer が発表されたのはいつで、論文の題は :: 2017 年で、題は「Attention Is All You Need」です。
Transformer が大規模言語モデルを可能にしたのはなぜですか :: 並列に計算できるため、GPU を並べればモデルとデータをどこまでも大きくできるからです。
\`\`\`
`,
  },

  {
    id: 'g-seq-3',
    categoryId: 'g-seq',
    title: 'オートエンコーダ',
    goal: 'オートエンコーダの仕組みと、VAE との違いを説明できる',
    minutes: 20,
    body: `
# ざっくり言うと

**オートエンコーダ**（auto＝自己、encoder＝符号化するもの。**自分自身を符号化して、また元に戻す**という意味の名前です）は、**一見すると何の役にも立たないことをする**ネットワークです。

**入力と同じものを出力するように学習させます。**

「猫の写真を入れたら、同じ猫の写真を出す」。**それの何が嬉しいのでしょうか。**

秘密は**途中の形**にあります。**真ん中に、わざと狭い層を置きます。**

\`\`\`diagram:flow
title: オートエンコーダの形
入力 :: 784 個の数値（28 かける 28 の画像）
エンコーダ :: だんだん狭くしていく
狭い層 :: たとえば 32 個。ここを通らなければならない
デコーダ :: だんだん広げていく
出力 :: 784 個。入力と同じになるよう学習する
\`\`\`

**784 個の情報を 32 個に詰め込み、そこから 784 個に戻す。** これができるということは、**32 個の数値に、元の画像の本質が詰まっている**ということです。

**細い管を通してものを運ぶには、余分なものを削ぎ落とすしかありません。** その「削ぎ落とし方」を学習させるのがオートエンコーダです。

# 何に使うのか

## 次元削減

いちばん素直な使い道です。**784 次元のデータを 32 次元に圧縮できます。**

[教師なし学習](textbook/d-ml-4)で見た主成分分析も次元削減の手法でしたが、**あちらは直線的な圧縮しかできません。** オートエンコーダは**ニューラルネットワークなので、曲がった圧縮ができます。** そのぶん複雑な構造を捉えられます。

**正解のラベルが要らない**点も重要です。入力そのものが正解になるので、**教師なし学習**として扱えます。

## 事前学習：積層オートエンコーダ

**いまは使われませんが、歴史的に重要な使い方**です。

[勾配消失問題](textbook/e-dl-2)のせいで深いネットワークが学習できなかった時代、**オートエンコーダを 1 層ずつ積み上げて学習させ、その重みを初期値として使う**という工夫がありました。これを**積層オートエンコーダ**による**事前学習**といいます。

\`\`\`diagram:flow
title: 積層オートエンコーダによる事前学習
1 層目をオートエンコーダとして学習 :: 入力を再現できるようにする
その出力を入力として 2 層目を学習 :: 同じことを繰り返す
これを層の数だけ繰り返す :: 1 層ずつ積み上げる
得られた重みを初期値にする :: そこから全体を学習し直す（ファインチューニング）
\`\`\`

**深いネットワークをいきなり学習させるのは無理でも、1 層ずつなら学習できる**——という発想です。**最後に全体をまとめて学習し直す段階を、ファインチューニング**といいます（[転移学習とファインチューニング](textbook/i-gen-3)で詳しく扱います）。

> **よくある勘違い** **積層オートエンコーダによる事前学習は、いまはほとんど使われていません。** ReLU、適切な初期化、バッチ正規化、スキップ結合といった工夫によって、**深いネットワークを最初から学習できるようになった**からです。「現在の標準的な手法」と書かれた選択肢は誤りです。

## 異常検知

**正常なデータだけでオートエンコーダを学習させる**と、**正常なものはうまく復元できるのに、異常なものはうまく復元できません。** この「復元の失敗具合」で異常を見つけられます。

# VAE：生成できるようにする

**ここが、この節でいちばん問われるところです。**

**通常のオートエンコーダは、生成モデルではありません。**

**生成モデル**とは、**学習したデータに似た、新しいデータを作り出せるモデル**のことです（[データ生成](textbook/i-gen-2)で詳しく扱います）。

理由を説明します。狭い層の 32 個の数値を、適当に決めて入れてみたらどうなるか。**たいていは意味のない出力になります。** 学習した画像に対応する「点」は覚えていますが、**その間の空間がどうなっているか**は保証されていないからです。

**変分オートエンコーダ（VAE）**は、ここを変えました。**狭い層を「点」ではなく「確率分布」として扱います。**

この**狭い層の値**を**潜在変数**（表には見えない、隠れた特徴を表す変数）といいます。VAE は、入力ごとに**1 つの点**ではなく、**平均とばらつき**を出します。そして、そこから**サンプリング**（＝その分布に従って値を 1 つ取り出すこと）して、デコーダに渡します。

**学習では、2 つのことを同時に見ます。**

1. **元に戻せているか**（復元の誤差）
2. **潜在変数の分布が、あらかじめ決めた形（ふつうは標準正規分布）に近いか**

**2 つめがあるおかげで、潜在変数の空間がすき間なく埋まります。** **だから、適当に取り出した値でも意味のある出力になるのです。**

なお、**サンプリングを含むと、そのままでは誤差逆伝播ができません。** そこで、**乱数を外から与える形に式を書き換える**工夫が使われます。**再パラメータ化**といいます。

\`\`\`diagram:compare
title: オートエンコーダと VAE
actors: 通常のオートエンコーダ | 変分オートエンコーダ（VAE）
狭い層は 1 つの点 :: 入力ごとに 1 点が決まる
狭い層は確率分布 :: 平均とばらつきを出す
点と点のあいだは保証されない :: 適当な値を入れても意味のある出力にならない
分布からサンプリングできる :: 適当に取り出しても意味のある出力になる
生成モデルではない :: 新しいデータを作れない
生成モデルである :: 新しいデータを作れる
\`\`\`

**分布として学習させると、その分布から適当に取り出した値でも、意味のある出力になります。** だから**新しいデータを生成できます。**

> **試験のポイント** **通常のオートエンコーダは生成モデルではなく、VAE は生成モデル。** 違いは、**潜在変数（狭い層）を点として扱うか、確率分布として扱うか**です。

## VAE の仲間たち

シラバスには、VAE の変種が 3 つ挙がっています。**名前と、何を変えたかの組**で押さえてください。

| 名前 | 何を変えたか |
| --- | --- |
| **VQ-VAE** | 潜在表現を**とびとびの値（離散）に量子化**する。VQ は Vector Quantization（ベクトル量子化）の略 |
| **β-VAE** | 上の 2 つめ（**分布を決めた形に近づける項**）に**重み β を付ける**。特徴を独立した意味の軸に分けやすくなる |
| **info VAE** | 入力と潜在変数のあいだの**情報量**に着目して改良したもの |

**VQ-VAE の「離散化」**は、**言葉のように「とびとび」のものを扱うのに向いています。** 画像生成や音声生成の基盤にもなりました。

**β-VAE の狙い**は、**潜在変数の各軸が、それぞれ意味のある特徴に対応するようにする**ことです。「この軸を動かすと顔の向きが変わる」「この軸は表情」というように分かれると、**扱いやすく、解釈しやすくなります。**

# この節のまとめ

- **オートエンコーダは、入力と同じものを出力するように学習させる**ネットワーク。**真ん中に狭い層を置く**のが要点
- 狭い層を通すことで、**データの本質を少ない数値に圧縮する方法**を学習する
- 用途は**次元削減**（主成分分析と違い、曲がった圧縮ができる）、**事前学習**、**異常検知**
- **正解のラベルが要らない**ので、教師なし学習として扱える
- **積層オートエンコーダによる事前学習は歴史的なもので、いまはほとんど使われていない**
- **生成モデル**とは、**学習したデータに似た新しいデータを作り出せるモデル**のこと
- **通常のオートエンコーダは生成モデルではない。** 潜在変数を「点」として扱うため、間の空間が保証されない
- **VAE の学習は「元に戻せているか」と「潜在変数の分布が決めた形に近いか」の 2 つを同時に見る**
- **変分オートエンコーダ（VAE）は潜在変数を確率分布として扱う**ので、**そこからサンプリングして新しいデータを生成できる**
- **VQ-VAE** は潜在表現を離散化、**β-VAE** は分布をそろえる項に重みを付けて意味の軸を分けやすくする、**info VAE** は情報量に着目した改良

\`\`\`quiz
オートエンコーダは何を学習しますか :: 入力と同じものを出力することを学習します。真ん中に狭い層を置くことで、データの本質を少ない数値に圧縮する方法を学びます。
オートエンコーダが教師なし学習として扱えるのはなぜですか :: 入力そのものが正解になるため、人がラベルを用意する必要がないからです。
オートエンコーダによる次元削減は、主成分分析と何が違いますか :: 主成分分析は直線的な圧縮しかできませんが、オートエンコーダはニューラルネットワークなので曲がった圧縮ができます。
積層オートエンコーダによる事前学習は、いまも標準的に使われていますか :: いいえ。ReLU や適切な初期化、バッチ正規化などで深いネットワークを最初から学習できるようになり、ほとんど使われていません。
オートエンコーダを異常検知に使う方法は :: 正常なデータだけで学習させると、正常なものはうまく復元でき、異常なものは復元に失敗するため、その失敗具合で異常を見つけられます。
通常のオートエンコーダは生成モデルですか :: 違います。潜在変数を点として扱うため、適当な値を入れても意味のある出力になりません。
VAE は何を変えて生成できるようにしましたか :: 潜在変数を点ではなく確率分布として扱うようにし、その分布からサンプリングできるようにしました。
生成モデルとは何ですか :: 学習したデータに似た、新しいデータを作り出せるモデルのことです。
VAE の学習では何を同時に見ますか :: 元に戻せているか（復元の誤差）と、潜在変数の分布があらかじめ決めた形に近いかの 2 つです。
再パラメータ化は何のための工夫ですか :: サンプリングを含むと誤差逆伝播ができないため、乱数を外から与える形に書き換えて学習できるようにする工夫です。
VQ-VAE の VQ は何の略で、何をしますか :: Vector Quantization（ベクトル量子化）の略で、潜在表現をとびとびの値に離散化します。
β-VAE の狙いは何ですか :: 分布をそろえる項に重みを付けることで、潜在変数の各軸がそれぞれ意味のある特徴に対応しやすくすることです。
\`\`\`
`,
  },

  {
    id: 'g-seq-4',
    categoryId: 'g-seq',
    title: 'データ拡張',
    goal: 'データ拡張の目的と、代表的な手法を説明できる',
    minutes: 18,
    body: `
# ざっくり言うと

[過学習への対処](textbook/e-dl-4)で、**「いちばん効くのはデータを増やすこと」**と書きました。

ところが、データを集めるのは**費用も時間もかかります。** 医療画像なら、そもそも症例が限られています。

そこで、**手元のデータを加工して水増しする**のが**データ拡張**（data augmentation）です。

猫の写真を左右反転させても、**それはやはり猫の写真**です。少し明るくしても、少し傾けても、猫は猫。**1 枚から何十枚も作れます。**

> **試験のポイント** **データ拡張の目的は、訓練データを水増しして過学習を防ぎ、汎化性能を上げること。** 「学習を速くするため」ではありません。

# 画像に対する基本的な手法

シラバスに挙がっている手法を、**性質ごとに整理**します。

## 位置や向きを変える

| 名前 | 何をするか |
| --- | --- |
| **Crop** | 画像の一部を切り取る |
| **Random Flip** | 左右（または上下）に反転する |
| **Rotate** | 回転させる |

**「被写体が画面のどこにあっても、どの向きでも認識できるように」**という狙いです。

## 色や明るさを変える

| 名前 | 何をするか |
| --- | --- |
| **Brightness** | 明るさを変える |
| **Contrast** | 明暗の差の強さを変える |
| **noising** | ノイズ（ざらつき）を加える |

**「撮影条件が違っても認識できるように」**という狙いです。曇りの日の写真も、暗い室内の写真も、同じ物として認識してほしいわけです。

## 一部を隠す

| 名前 | 何をするか |
| --- | --- |
| **Cutout** | 画像の一部を四角く塗りつぶして隠す |
| **Random Erasing** | 画像の一部をランダムな値で置き換えて隠す |

**「一部が隠れていても認識できるように」**という狙いです。実世界では、物は柱や人に隠れて一部しか見えないことがよくあります。

## 2 枚を混ぜる

**この 2 つは取り違えやすいので、はっきり分けてください。**

\`\`\`diagram:compare
title: Mixup と CutMix
actors: Mixup | CutMix
2 枚を半透明に重ねる :: 画像全体をなめらかに混ぜる
一方の一部を切り取って他方に貼る :: 四角い領域を差し替える
ラベルも同じ比率で混ぜる :: 猫 0.7、犬 0.3 のような正解にする
ラベルは面積の比率で混ぜる :: 貼った面積の分だけ混ぜる
できあがりは不自然な半透明画像 :: 現実には存在しない見た目
できあがりは切り貼りした画像 :: 部分ごとには自然な見た目
\`\`\`

- **Mixup**……**2 枚の画像を半透明に重ね、ラベルも同じ比率で混ぜます。** 「猫 70 %・犬 30 %」という正解を与えるわけです
- **CutMix**……**一方の画像の一部を切り取って、他方に貼り付けます。** ラベルは**貼った面積の比率**で混ぜます

**どちらも「正解を 1 つに決めない」**という点が共通しています。**モデルが自信を持ちすぎるのを防ぐ**効果があるとされます。

## 手法の選び方も自動化する

**RandAugument** は、**適用する手法をランダムに選び、強さは共通の値で指定する**手法です。**「いくつ適用するか」と「どれくらいの強さで適用するか」の 2 つだけを決めれば済みます。**

データ拡張には「どの手法を、どの強さで、どの順で使うか」という選択があり、**これを人が探すのは大変**です。**RandAugument は、探すべきつまみを 2 つに減らします。**

**なお、原論文の綴りは RandAugment（u が 1 つ少ない）**です。**シラバスの表記に合わせて RandAugument と書いていますが、原典の名前は RandAugment です。**

# 文章に対するデータ拡張

画像ほど単純ではありませんが、自然言語にも拡張の方法があります。

**paraphrasing** は**言い換え**です。「明日は雨が降ります」を「明日は雨模様でしょう」に変えるように、**意味を保ったまま表現を変えます。**

**文章は少し変えただけで意味が変わってしまう**ので、画像より慎重さが要ります。

# 何でも拡張してよいわけではない

**この節でいちばん大事な注意点**です。

**データ拡張は「変えても意味が変わらない」場合にしか使えません。**

\`\`\`diagram:compare
title: 使ってよい場面・いけない場面
actors: 使ってよい | 使ってはいけない
猫の写真を左右反転する :: 反転しても猫は猫
数字の 6 を 180 度回転する :: 9 に見えてしまう
風景写真の明るさを変える :: 撮影条件の違いにすぎない
医療画像の明暗を強く変える :: 病変の見え方そのものが変わりうる
一般物体を回転させる :: どの向きでもその物体
文字を左右反転する :: 別の文字か、存在しない文字になる
\`\`\`

> **よくある勘違い** **「データ拡張は多いほどよい」わけではありません。** データの性質に合わない拡張は、**間違った正解を大量に作ることになり、精度をむしろ下げます。** どの拡張を使うかは、**扱うデータの意味を考えて選ぶ**必要があります。

**特に反転と回転は要注意**です。数字、文字、標識、医療画像など、**向きに意味があるデータでは使えません。** **6 を 180 度回すと 9 に見えてしまいますし、文字を左右反転すれば別の文字か、存在しない文字になります。**

**もう 1 つ、適用する範囲の注意**があります。

> **よくある勘違い** **データ拡張をかけるのは、訓練データだけです。** **検証データやテストデータには行いません。** **これらは「本番と同じ条件で性能を測る」ためのもの**なので、加工してしまうと**測りたいものが測れなくなります。**

# この節のまとめ

- **データ拡張は、手元のデータを加工して水増しする**手法。目的は**過学習を防ぎ、汎化性能を上げること**
- 位置や向きを変える手法は **Crop・Random Flip・Rotate**
- 色や明るさを変える手法は **Brightness・Contrast・noising**
- 一部を隠す手法は **Cutout・Random Erasing**
- **Mixup は 2 枚を半透明に重ねてラベルも混ぜる。CutMix は一方の一部を切り取って他方に貼り、ラベルを面積比で混ぜる**
- **RandAugument** は、どの手法をどの強さで使うかをランダムに選び、探索の手間を減らす
- 文章では **paraphrasing**（言い換え）が使われるが、画像より慎重さが要る
- **データの性質に合わない拡張は精度を下げる。** 特に**向きに意味があるデータ（数字・文字・標識）で反転を使ってはいけない**

\`\`\`quiz
データ拡張の目的は何ですか :: 手元のデータを加工して水増しし、過学習を防いで汎化性能を上げることです。
位置や向きを変えるデータ拡張を 3 つ挙げてください :: Crop（切り取り）、Random Flip（反転）、Rotate（回転）です。
Cutout や Random Erasing はどんな狙いの手法ですか :: 画像の一部を隠すことで、物が一部隠れていても認識できるようにする狙いです。
Mixup とはどんな手法ですか :: 2 枚の画像を半透明に重ね、ラベルも同じ比率で混ぜる手法です。
CutMix とはどんな手法ですか :: 一方の画像の一部を切り取って他方に貼り付け、ラベルを貼った面積の比率で混ぜる手法です。
Mixup と CutMix に共通する狙いは :: 正解を 1 つに決めないことで、モデルが自信を持ちすぎるのを防ぐことです。
RandAugument は何を自動化しますか :: どの拡張手法をどれくらいの強さで使うかの選択をランダム化し、人が探す手間を減らします。
文章に対するデータ拡張の例は :: paraphrasing、つまり意味を保ったまま表現を変える言い換えです。
上下反転を使ってはいけないのはどんなデータですか :: 数字や文字、標識のように向きに意味があるデータです。6 が 9 に見えるなど、意味が変わってしまいます。
データ拡張は多ければ多いほどよいですか :: いいえ。データの性質に合わない拡張は間違った正解を大量に作ることになり、精度をむしろ下げます。
\`\`\`
`,
  },
];
