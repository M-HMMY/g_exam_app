# CLAUDE.md

このファイルは、このリポジトリで作業するときに最初に読む前提です。

## このアプリは何か

G検定（JDLA Deep Learning for GENERAL）の学習アプリ。**教本を軸にした学習アプリ**であって、問題集アプリではありません。

土台は `fe_exam_app`（基本情報技術者試験）→ `e_exam_app`（E資格）→ `itpassport_exam_app`（ITパスポート）と引き継いできたものです。**コードは `C:\Dev\itpassport_exam_app` から持ってきてください。**系譜のなかでいちばん新しく、検査もいちばん充実しています。画面・記法・運用の作法は共通なので、迷ったら向こうの実装を見れば答えがあります。

## 最優先の方針

**「通読できる教本」が最優先の要件です。** 演習機能より教本の質を優先してください。

その上で、G検定の読者は**姉妹アプリのどちらとも違う**位置にいます。ここを取り違えると全部ずれます。

| アプリ | 読者 | 数式の扱い |
| --- | --- | --- |
| `itpassport_exam_app` | IT の知識がゼロ | ほぼ出さない。出すなら導出から |
| **このアプリ** | **IT の基礎語彙はあるが、エンジニアではない** | **出す。ただし「読めれば足りる」** |
| `e_exam_app` | 認定プログラム修了済みのエンジニア | 出す。実装まで踏み込む |

G検定は受験資格がなく、実際に受けるのは事業部門の社会人、企画職、学生、これから AI を扱う人です。**「サーバ」「データベース」は説明なしで通じますが、「勾配」「尤度」は通じません。**

書くときの基準は次のとおりです。

- **AI・機械学習の用語は、初めて出したその場で必ず言い換える。** IT 一般の用語は前提にしてよい
- **数式は「読み方」を添える。** シグマが出たら「全部足す、という意味」と書く。**解かせない。変形させない。** `e_exam_app` の入門編「数式の読み方」が下敷きに使えます
- **人名・モデル名は、それが何を変えたかとセットで書く。** 名前だけ並べても覚えられません
- **たとえ話を最初に置く。** 抽象的な定義はそのあと
- **カタカナ語には、なぜその名前なのかを添える。** 「アテンション（attention＝注意。どこに注目するかを重みで決める）」

判断に迷ったら「**文系の社会人が、数式を飛ばさずに読み切れるか**」を基準にしてください。

## 試験の形（教本の前提）

**2026 年 9 月 2 日に [JDLA 公式](https://www.jdla.org/certificate/general/) で確認した内容です。**要項は改訂されるので、作業を始める前にもう一度見てください。教本の中でも断定せず、「最新の情報は JDLA の公式サイトで確認」と促してください。

| 項目 | 内容 |
| --- | --- |
| 形式 | 多肢選択式のみ。記述も実技もない |
| 出題数 | **145 問程度**（2026 年第 1 回で 160 問程度から変更） |
| 試験時間 | **オンライン 100 分 / 会場 120 分**（同じ問題数で時間が違う） |
| 実施 | オンライン 年 6 回 / 会場 年 3 回 |
| 受験資格 | なし |
| 受験料 | 一般 13,200 円 / 学生 5,500 円（再受験は半額、AI For Everyone 修了者は 30 % 引き） |
| 合格基準 | **非公表** |
| シラバス | G2024#6〜（2024 年 11 月適用の改訂版。生成 AI の技術が新規追加された）。**現行は第 1.4 版・2026 年 5 月 11 日改訂** |

書き起こしを [docs/syllabus-g2024.md](docs/syllabus-g2024.md) に置いてあります（中項目 55 個・キーワード 495 語の全文と、この教本の章との対応表）。章を書くときはこれを読ませてください。ただし**シラバスは改訂されます。**版が上がっていないか公式サイトで確かめること。

### この試験の形が設計に効く 3 点

**1. 1 問あたり約 41 秒しかない。**

オンラインなら 145 問を 100 分。ITパスポートは 100 問を 120 分（1 問 72 秒）なので、**7 割ほどの持ち時間**しかありません。会場受験は同じ 145 問で 120 分（1 問 約 50 秒）です。

つまり、**考えて解く試験ではなく、用語を即座に思い出せるかの試験**です。ここから次が決まります。

- **一問一答（` ```quiz `）と用語辞典の価値が、ITパスポート版より高い**
- **長い計算問題の価値は低い。** 計算ドリルを作るとしても、本番の主役ではない
- **問題文を長くしない。** 本番で読み切れない長さの問題は、練習として不誠実です

**2. 合格基準が非公表で、分野別の足切りも公表されていない。**

ITパスポートは「総合 600 点かつ 3 分野それぞれ 300 点」という構造がアプリ設計の前提でした。**G検定にはそれがありません。**

`src/pages/Mock.tsx` は作り直し済みです。**合否を判定せず、点数への換算もしません。**出すのは次の 3 つだけ。

- 正答率（「合格圏」などの判定は出さない）
- **時間内に解き切れたか**（未解答数と、1 問あたりの実測秒数を持ち時間と比べる）
- 分野別・章別の正答率（どこに時間を使うか決めるため）

**非公表の基準を、アプリが勝手に「合格」と断定しないこと。**この方針を崩す変更を入れないでください。

出題数の内訳も**公表されていません。**模試の構成比は、シラバスのキーワード数から按分した推定値（`categories.ts` の `questions`）です。**本番の内訳ではないと、コメントにも画面にも書いてあります。**

**3. 範囲が広く浅い。人名・モデル名・法律名が大量に出る。**

検索（`src/lib/search.ts`）と用語ミニ辞典が主役級の機能になります。用語辞典の節 id は `src/data/categories.ts` の **`GLOSSARY_SECTION_ID`（現在 `i-4`）** で、`Textbook.tsx` はこの定数を参照します（ITパスポート版のように画面が id を直書きしていません）。数式の読み方の節も **`MATH_SECTION_ID`（現在 `i-3`）** で同様に持っています。**入門編を書くときは、この 2 つの id を必ず実在させてください。**

## 出題範囲（シラバス 2024 第 1.4 版）

**公式 PDF を確認済みです。**シラバスは 2 分野に分かれています。

- **【技術分野】** 大項目 8 個 / 中項目 37 個 / キーワード 411 語
- **【法律・倫理分野】** 大項目 2 個 / 中項目 18 個 / キーワード 84 語

「AI に必要な数理・統計知識」は**独立した大項目**でした（技術分野の中項目 37 番。1 項目にキーワード 29 語）。他章には溶けていません。

全文は [docs/syllabus-g2024.md](docs/syllabus-g2024.md) にあります。

### 章立て（確定）

大項目そのままではありません。分量が突出する 2 つを割り、数理・統計を機械学習の前に繰り上げてあります。

| 章 ID | 章名 | シラバス | 推定出題数 |
| --- | --- | --- | --- |
| `intro` | はじめに | 範囲外 | — |
| `a-what` | 人工知能とは | 技術 1〜2 | 5 |
| `b-trend` | 人工知能をめぐる動向 | 技術 3〜6 | 11 |
| `c-math` | AI に必要な数理・統計知識 | 技術 37 | 8 |
| `d-ml` | 機械学習の概要 | 技術 7〜10 | 18 |
| `e-dl` | ディープラーニングの概要 | 技術 11〜16 | 15 |
| `f-layer` | 要素技術（1）画像を扱う層 | 技術 17〜21 | 6 |
| `g-seq` | 要素技術（2）系列・生成・データ拡張 | 技術 22〜25 | 12 |
| `h-recog` | 応用例（1）画像・言語・音声 | 技術 26〜28 | 21 |
| `i-gen` | 応用例（2）強化学習・生成・実用化 | 技術 29〜34 | 17 |
| `j-social` | AI の社会実装に向けて | 技術 35〜36 | 7 |
| `k-law` | AI に関する法律と契約 | 法律・倫理 1〜7 | 12 |
| `l-ethics` | AI 倫理と AI ガバナンス | 法律・倫理 8〜18 | 13 |

**「推定出題数」はキーワード数から 145 問を按分しただけの目安です。**JDLA は内訳を公表していません。

`src/data/textbook/<章 ID>.ts` と `src/data/questions/<章 ID>.ts` が 1 章 1 ファイルで対応します。**複数のエージェントを並行で走らせても、1 ファイル 1 担当なら衝突しません。**

## 変えないこと

- **ランタイム依存は React だけ。** ルータ・Markdown・数式・図はすべて自前。ライブラリを足す前に、本当に必要か検討する
- **外部通信をしない。** 学習記録は localStorage のみ。サーバもアカウントもない
- **オフラインで動く。** PWA として成立させる
- TypeScript は strict（未使用変数もエラー）

## 節の書き方

本文は Markdown サブセットです。1 つの節は次の骨格で書きます。

```
# ざっくり言うと          ← 日常語で「何の話か」。式は出さない
（本論。見出し・表・図・数式）
> **試験のポイント** …     ← 暗記すべき事項
> **よくある勘違い** …     ← 先回りして潰す誤解
# この節のまとめ           ← 箇条書き 5〜6 個
```quiz                    ← 一問一答（採点も記録もしない）
問い :: 答え
```
```

「この節のまとめ」「試験のポイント」「よくある勘違い」の 3 つは、`src/lib/digest.ts` が自動で抜き出して**直前チェックシート**に並べます。**書式を崩すと拾われません。**

**この試験は 1 問 41 秒なので、` ```quiz ` を厚めに入れてください。**直前チェックシートと一問一答が、そのまま本番の速度対策になります。`npm run check` が、骨格の 3 つ（「# ざっくり言うと」「# この節のまとめ」「> **試験のポイント**」）と一問一答の数（5 問未満で注意）を数えます。

### 使える記法

`itpassport_exam_app` と同じです。`compare` 図は「1 行 1 セル、偶数行が左・奇数行が右」で書きます（要素が奇数だと `npm run check` がエラーにします）。図の中では `**強調**` が効きません。

数式は ITパスポート版より出番が多くなります。使える記号は `src/lib/mathSymbols.ts` にあります。**`e_exam_app` 側と突き合わせ済みで、あちらにしかない命令はありませんでした。**それでも足りなければ足してください（表に無い命令は、記号にならず名前がそのまま画面に出ます。`npm run check` がエラーにします）。

### バックスラッシュの落とし穴（重要）

本文は TypeScript の**テンプレートリテラル**の中にあります。

| 書きたいもの | ファイルに書く形 |
| --- | --- |
| `\times` | `\\times` |
| バックティック | バックスラッシュ + バックティック |
| `${` | `\${` |

さらに、**bash のヒアドキュメント（`cat > file <<'EOF'`）はバックスラッシュを勝手に落とします。** 姉妹アプリで何度も壊しました。Write ツールを使うか、`scripts/emit_textbook.py` を使ってください。**数式が多いこのアプリでは、ITパスポート版より踏みやすい落とし穴です。**

## 問題の書き方

- **`sectionId` は必ず付ける。** 解説から教本へ戻る導線と、節ごとの出題に使う
- 選択肢は 4 つ。**正解の位置は散らす**
- 解説では、正解の理由だけでなく**誤答がなぜ誤りか**にも触れる
- **誤答も正解と同じ密度で書く。** 正解だけが長いと、読まずに「長いものを選ぶ」で当てられます
- **「必ず」「すべて」を誤答専用にしない。** 言い切りが誤答側にしか出てこないと、それ自体が手掛かりになります
- **問題文を長くしない。** 本番は 1 問 41 秒です。`npm run check` が、空白を除いて 120 字で注意・160 字でエラーにします

上の 3 つは `npm run check` が数えて注意を出します。

## 引き継ぐコード

`C:\Dev\itpassport_exam_app` から持ってきます。**中身を見ずにフォルダごとコピーしないでください。**下の 3 分類のうち「直す」に入っているものは、ITパスポートの前提が埋まっています。

### そのまま使えるもの

- `src/lib/` 全部（router / markdown / math / mathSymbols / digest / srs / stats / storage / search / useKeys）
- `src/components/` の `ChoiceList` `Diagram` `QuestionCard` `SelfCheck` `Widget`
- `src/store.ts` `src/styles.css`
- `scripts/check.ts` `scripts/render-check.tsx` `scripts/emit_textbook.py`
- `public/sw.js`
- `.github/workflows/deploy.yml`
- `package.json` `vite.config.ts` `tsconfig*.json`

### 直す必要があるもの

| ファイル | 何が埋まっているか |
| --- | --- |
| `src/pages/Mock.tsx` | **600 点 / 300 点の合否判定。作り直しが要る**（上記のとおり） |
| `src/data/categories.ts` | 分野と章の定義。全書き換え |
| `src/types.ts` | `FieldId` が `'intro' \| 'strategy' \| 'management' \| 'technology'` |
| `src/pages/Textbook.tsx` | **`i-4` が用語辞典**という前提を直接持っている |
| `src/pages/Home.tsx` `src/App.tsx` | 試験名と分野名 |
| `index.html` `public/manifest.webmanifest` `public/icons/` | アプリ名とアイコン |
| `scripts/launch.ps1` と `launch.cmd` | タイトル文字列。**対応済み**（`launch.cmd` は新規作成） |
| `scripts/prompts/` | 章ごとの執筆プロンプト。**`90-review.md` と `91-review-questions.md` は構造を残して中身を差し替える** |

### 捨てるもの（G検定の中身に置き換わる）

`src/data/textbook/` `src/data/questions/` `src/data/drills.ts` `src/components/widgets/`

**ウィジェットは `src/components/widgets/<id>.tsx` を置くだけで自動登録されます。**ただし `scripts/render-check.tsx` の `WIDGETS` にも足してください。足し忘れると描画の検査だけ素通りします。

## 作業の作法

### 必ず通すもの

```
npm run check   # データの整合性 + 実際に描いてみる検査
npx tsc --noEmit
npm run build
```

`npm run check` は「型では防げない食い違い」を捕まえます。ID 重複、参照切れ、記法の崩れ、図の書式、リンクの飛び先、数式のエスケープ漏れ、ドリルの選択肢、節をまたいだ問題の重複、そして**実際に React で描画して NaN や崩れが出ないか**まで見ます。**新しい不整合の型を見つけたら、直すついでに `scripts/check.ts` へ検査を足してください。**

### 内容の正しさ

**書いた本人が読み返しても間違いは見つかりません。** 系譜での実績は次のとおりです。

| アプリ | 対象 | 別の目で見つかった誤り | 自己レビューで見つかった数 |
| --- | --- | --- | --- |
| `fe_exam_app` | 教本 約 7 万字 | 16 件（重大 4 件） | 0 件 |
| `itpassport_exam_app` | 教本 52 節 | **90 件超（重大な事実誤り 30 件）** | 0 件 |
| `itpassport_exam_app` | 確認問題 334 問 | **94 件** | 0 件 |
| `genai_passport_exam_app` | 確認問題 180 問 | **68 件** | 0 件 |
| `g_exam_app` | 教本 59 節 | **200 件超**（2 回に分けて実施） | 0 件 |
| **`g_exam_app`** | **確認問題 435 問** | **154 件** | 0 件 |

まとまった量を書いたら、**書いた人とは別の目で検証する工程を必ず入れてください。**省略できる工程ではありません。

**G検定に固有の検証観点が 2 つあります。**

1. **情報が古くなっていないか。** 生成 AI の領域は半年で変わります。モデル名、性能、できることの記述は、**「いつ時点の話か」を本文に書いてください。**姉妹アプリにはなかった問題です
2. **法律が現行のものか。** 「AI の法律と倫理」が丸ごと 1 章あります。ITパスポート版では、著作権の保護期間や産業財産権の存続年数で**実際に誤りが出ました。**AI 事業者ガイドライン、著作権法 30 条の 4、個人情報保護法、EU AI Act などは改訂が続く領域です。**年数と条番号は、書いた人以外が一次資料で確かめること**

### レビューで実際に出た誤りの型

`itpassport_exam_app` で出たものです。**同じ型は繰り返し起きるので、書くときも直すときも意識してください。**

教本で出た型：

- **法改正の年をまたいだ誤り** … 改正前の年数を書いていた
- **「まとめて言い切る」ことで生まれる誤り** … 4 つの権利を一律に説明したが、1 つだけ例外があった
- **節をまたいだ食い違い** … 同じ単位を、ある節では 1,024 倍、別の節では 1,000 倍で扱っていた
- **本文と図の食い違い** … 図の軸が本文と違っていた
- **例と一般論の食い違い** … 直前の具体例と、一般化した説明の数が合っていなかった
- **図の名前と中身の食い違い** … 別の図法を描いて元の名前で呼んでいた
- **アプリの実装との食い違い** … 教本の説明が実装の挙動と違っていた
- **説明なしの初出** … 定義より前に用語が出ていた

問題で出た型：

- **前提条件の書き落とし** … 計算問題で、教本には書いてある前提が問題文になかった
- **誤答が実は正しい** … 言い切りすぎて誤答にしたつもりが、実際には正しかった
- **分類の軸が混ざっている** … 誤答が正解と別の軸の言葉で、両方成り立ってしまった
- **かっこ書きの言い換えが別語だった** … 同義語だと思って添えた語が、別の意味の専門用語だった
- **語源の思い込み** … もっともらしいが違う由来を書いていた
- **条件の列挙もれ** … 成立条件が 1 つ落ちていた
- **上限と下限の取り違え**
- **章をまたいだ重複** … 同じ数値の計算問題が 2 章にあった。1 節ずつ見ている限り気づけない
- **誤答の説明もれ** … 4 つのうち 1 つだけ解説で触れていない。**最も多い型**

逆に、**レビュー側が範囲外の指摘をしてくる**こともあります。ITパスポートでは、範囲外の項目を「重大な欠落」として挙げてきました。**プロンプトでこちらが例示した語が、そのまま誘導になります。採否はこちらで判断してください。**

なお、**「解説が誤答に触れているか」を文字列一致で機械的に測るのは無理です。**言い換えて触れているものを全部見逃しと数えてしまいます（334 問中 233 問が誤検出でした）。同じ検査を作り直さないでください。

### 公開のルール

公開リポジトリにする前提です。

- **コミットの署名は GitHub の noreply アドレスを使う。** 実メールアドレスはコミット履歴に永久に残ります
- 個人情報・ローカルパス・API キーを含めない
- **`.github/workflows/deploy.yml` の対象ブランチを、実際のブランチ名に合わせること。** ITパスポート版では `main` を待つ設定なのに既定ブランチが `master` で、**一度も動いていませんでした**。**このリポジトリは `git init` 済みで、既定ブランチは `main` です。**持ってきた時点の `deploy.yml` は `master` を待つ設定だったので、`main` に直してあります。**ブランチ名を変えるときは、この 2 つを必ず一緒に動かすこと**

## 章を書かせるやり方

**Codex CLI を使います**（`npm install -g @openai/codex`、ChatGPT ログイン済み）。姉妹アプリの教本の大半はこれで書きました。

```bash
codex exec --cd "C:/Dev/g_exam_app" --sandbox workspace-write --color never -o out.txt - < scripts/prompts/03-xxx.md
```

依頼側のコンテキストを食わないよう、`-o` で最終メッセージだけをファイルに落とすのが要点です。実際に使ったプロンプトは `itpassport_exam_app/scripts/prompts/` に全部残っています。**そこの README を読んでひな形にしてください。**何をプロンプトに入れると品質が上がるか（間違えやすい事実の名指し、リンクしてよい節 id の全列挙など）が書いてあります。

**章のプロンプトには [docs/syllabus-g2024.md](docs/syllabus-g2024.md) の該当箇所を読ませてください。**その章が担当する中項目の「目標」と「キーワード」が、そのまま書くべき内容の外枠になります。

レビューは `90-review.md`（教本用）と `91-review-questions.md`（確認問題用）が対になっています。**教本用をそのまま問題に使わないでください。**教本は「書いてあることが正しいか」ですが、問題は「**正解が本当に 1 つに定まるか**」が最大の論点で、見る点が違います。

**Codex には利用上限があります。** ITパスポート版では、複数ジョブを並行させて上限に当たり、**ファイルを編集し終えた直後・報告を書く前に停止した**ことがあります。編集は残るのに報告が残らないので、何が終わっていて何が終わっていないか分かりません。**上限に当たったら、`grep -c` などで期待する語を数えて、実際に反映されたかを確かめてください。**

## いまの状態

| 項目 | 状況 |
| --- | --- |
| シラバスの確認 | **済**。第 1.4 版を [docs/syllabus-g2024.md](docs/syllabus-g2024.md) に書き起こし済み |
| 試験要項の確認 | **済**（2026 年 9 月 2 日）。145 問 / オンライン 100 分・会場 120 分 |
| 骨格の移植 | **済**。`npm run check` / `npx tsc --noEmit` / `npm run build` の 3 つが通る |
| 章立て | **確定**（13 章）。`src/data/categories.ts` |
| 節割り | **確定**（全 59 節）。[docs/section-plan.md](docs/section-plan.md) |
| 模試の合否判定 | **作り直し済**。合否を判定せず、正答率・解答ペース・分野別正答率だけを出す |
| 教本の本文 | **全 13 章 59 節 執筆済み（約 28 万字 / 通読 21 時間）。** `npm run check` の未着手の章 0 |
| 章ごとの執筆プロンプト | **12 章分すべて済**。`scripts/prompts/01-a-what.md` 〜 `12-l-ethics.md` |
| 直前チェックシート | **全 59 節から抽出できている**（まとめ 512・試験のポイント 126・よくある勘違い 58） |
| 教本の通しレビュー | **全 13 章 完了。2 回に分け、指摘 200 件超のうち採用分を反映済み**（下記） |
| 体験ウィジェット | **3 つ実装済み・教本に埋め込み済み**（`confusion` → `d-ml-6`、`convolution` → `f-layer-2`、`activation` → `e-dl-2`）。`scripts/render-check.tsx` にも登録済み |
| 確認問題 | **全 12 章 435 問 執筆済み。** `npm run check` / `npx tsc --noEmit` / `npm run build` が通る |
| 確認問題の通しレビュー | **済**（2026 年 9 月 12 日）。`91-review-questions.md` を 7 ジョブに割って Codex。**154 件**の指摘。限定提供データの要件で正解が 2 つあった、MYCIN の成績が事実と逆だった、転移学習が「効きにくい例」として挙げた組合せが実際には効く例だった、など |
| 選択肢の長さの偏り | **是正済**（2026 年 9 月 12 日）。正解が最長の選択肢である問題が 245 / 435 問（期待値 20.6 % に対し 56.3 %）あった。見分けられる差（5 字以上）の 107 問をすべて直し、`check.ts` の検査も比ではなく字数の差で見るように変えた |
| 計算ドリル | **未着手**。`src/data/drills.ts` は補助関数だけ |
| アイコン | **済**。`scripts/make_icons.py` で `public/icons/`（PWA 用）と `scripts/app.ico`（ショートカット用）をまとめて作る。紺→青のグラデーションに白い G と橙の下線 |
| iPhone 対応 | **ローカル側は済**。PWA の資産・`theme-color`・safe-area は確認済み。**残るは HTTPS で公開すること**（下記） |
| git | **公開済**。`https://github.com/M-HMMY/g_exam_app`。既定ブランチは **`main`** で、`deploy.yml` も `main` に合わせてある |
| 実ブラウザでの動作確認 | **済**（2026 年 9 月 12 日）。`DEV_PORT=5175 node scripts/drive.mjs`（`npm run dev` を別に立てておく）。Edge を headless で起動し、Chrome DevTools Protocol で実際に押す。**追加インストールなし**。これで体験ツールの画面が既定で空のグループを開いていたことが見つかった |

### 節割りを変えるときの注意

節 ID は [docs/section-plan.md](docs/section-plan.md) で確定させてあります。**変更すると次がいっせいに壊れます。**

- 確認問題の `sectionId`
- 本文中の `[文字](textbook/節id)` リンク
- `src/data/categories.ts` の `GLOSSARY_SECTION_ID`（`i-4`）と `MATH_SECTION_ID`（`i-3`）
- 各章の執筆プロンプトに列挙した「リンクしてよい節 ID」

増やすときは章の末尾に足してください。

## 教本の通しレビューの結果

**2 回に分けて全 13 章を見ました。**

| 回 | 対象 | 走らせた本数 | 指摘 | 自己レビューでの発見 |
| --- | --- | --- | --- | --- |
| 1 回目 | 11 章 | 5 本 | **100 件超** | **0 件** |
| 2 回目 | `g-seq` `h-recog` `k-law` `l-ethics` | 4 本（1 章 1 担当） | **106 件** | **0 件** |

**系譜の実績どおり、書いた本人の読み返しでは 0 件だったところに、合わせて 200 件を超える指摘が出ました。**

### 1 回目（11 章）

採用して直した主なものは次のとおりです。**同じ型が繰り返し出るので、書くときも直すときも意識してください。**

| 型 | 実例 |
| --- | --- |
| **事実の誤り** | VGG を「ILSVRC 優勝モデル」の図に入れていた（2014 年の分類優勝は GoogLeNet、VGG は 2 位）。α カット・β カットが G検定の教材と逆。ELIZA を「エキスパートシステムより少し前」としたが DENDRAL のほうが先 |
| **`compare` 図の誤用** | **1 対 1 の対応表のつもりで書いた 4 行が、偶数行が左・奇数行が右に振り分けられて意味が壊れる。** 要素数が偶数なので `npm run check` を素通りしていた。3 か所 |
| **数の食い違い** | 「1,000 の桁数は 3」（正しくは 4 桁、0 が 3 個）。「3 万個」と書いた直後に「1 万個」。「約 9 秒」（正しくは約 8 秒）。「2 分で 3 問落とす」（正しくは約 2 問） |
| **節をまたいだ食い違い** | 平均二乗誤差の式に 1/n がないのに「平均」と呼んでいた。第 1 次の冬の理由が章によって違う。第 3 次ブームの開始が「2010 年代」と「2000 年代」で割れていた |
| **本文と図の食い違い** | 本文に出てこない「台車」が図にある。図では勾配爆発側だけに寄せた項目を、一問一答では両方だとしていた |
| **言い切りすぎ** | 「移動平均は予測の手法ではない」（需要予測にも使われる）。「プルーニングは削っても精度が落ちない」（ふつう再学習で戻す）。宝くじ仮説から「初期値のまま」という条件が落ちていた |
| **時点の欠落** | 「いま最もよく使われる」「いまも強い」に時点がない。ローブナーコンテストは 2019 年を最後に開催されていない |
| **説明なしの初出** | CNN・Transformer・ニューラル機械翻訳が定義なしで出ていた |
| **描画の不具合** | `seq` 図の `actors` が行の表記と一致せず、**矢印が全行逆向きに描かれていた** |

**採らなかった指摘**もあります。ダートマス会議の語の初出（1955 年の提案書）は G検定の定番解答に合わせて会議のままにし、まとめの箇条書きが 5〜6 個を超える点は規約側の実態が違うと判断しました。**レビュー側の指摘をそのまま入れないでください。**

### 2 回目（残り 4 章）

**1 章 1 担当に割り、法律の 2 章には一次資料での裏取りを課しました。**指摘 106 件のうち、採用分を反映済みです。

| 型 | 実例 |
| --- | --- |
| **一度確かめた事実が、あとから古くなっていた** | 下記。**この章でいちばん重い発見** |
| **事実の誤り** | 2014 年 ILSVRC の分類優勝を GoogLeNet と書かずに済ませ、**compare 図で VGG を先に置いたため優勝モデルに見えていた**。**Attention が「前から順にしか読めない」制約まで外したと書いていた**（外したのは Transformer）。**6 を「上下反転」すると 9 になると書いていた**（正しくは 180 度回転）。**耳の感度は高い音ほど鈍る**と一般化していた（正しくは「周波数の違いを聞き分けにくくなる」） |
| **条文の要件の書き落とし** | 著作権法 30 条の 4 から**「又は他人に享受させる」が抜けていた。**そのため「2 つの限定」と書いていたが、実際は**享受目的の併存・必要な限度・ただし書の 3 つの関門**。**生成 AI で問題になるのはまさに落とした部分** |
| **例が日本語で成立していない** | word2vec の多義語の例に **bank（川岸／銀行）をそのまま日本語化**し、「土手」と「銀行」という**別々の語**の対比になっていた。**多義語の例として機能していない** |
| **図の幅・向きが意味と逆** | `stack` は**先頭が最も幅広い**。「個人情報 → 個人データ → 保有個人データ」を義務の重い順に並べたため、**最も狭い集合が最も広く描かれていた。** EU AI Act のリスク 4 段階も同じ |
| **`compare` 図の誤用** | **1 回目と同じ型がまた 1 件**（公平性の 4 つの定義を「考え方 ｜ 何を等しくするか」の対応表のつもりで書いていた） |
| **本文がソースの行番号を指していた** | 「最大の問題は **3 行目**です」。`compare` は左右に振り分けられるので、**読者の画面に 3 行目は存在しない** |
| **「よくある勘違い」が勘違いでない** | YOLO の項が**事実の再掲**になっていた。`digest.ts` が直前チェックシートに拾うので、**シートに勘違いでないものが並んでいた** |
| **章の構成と本文の食い違い** | 「この章の残りは、この価値ごとに 1 節ずつ」と書いてあるが、**価値は 6 つ・節は 4 つ** |
| **固有名詞を伏せて出題に使えない** | 公平性の事例を「ある企業の採用支援 AI」と匿名で書いていた。**G検定は事例を名前で問う**（COMPAS、Gender Shades など） |
| **シラバスにあるのに教本にない** | 人間中心の AI 社会原則、AI 事業者ガイドラインの 3 主体と 10 指針、産業財産権の存続期間、差分プライバシー・連合学習・秘密計算、公平性の定義の正式名称。**いずれも教本全体で 0 件だった** |

#### 一度確かめた事実が、あとから古くなっていた（この型を憶えておくこと）

**教本には「EU AI Act の高リスク AI への義務は 2026 年 8 月 2 日に全面適用された」と書いてありました。**作業前に確認して書いた内容です。**レビュー担当がこれを否定し、一次資料で確かめたところ、指摘が正しいものでした。**

**規則 (EU) 2026/1744（AI に関するデジタル・オムニバス）が AI Act 113 条を改正**しています。**2026 年 7 月 24 日 官報公表・7 月 27 日発効**で、**もとの期限の 6 日前**でした。

| 対象 | 適用日 |
| --- | --- |
| 附属書 III の**単独型**高リスク AI | **2027 年 12 月 2 日**（延期後） |
| 附属書 I の**製品組込型**高リスク AI | **2028 年 8 月 2 日**（延期後） |
| 2026 年 8 月 2 日に実際に適用されたもの | **50 条の透明性義務**ほか（高リスクの義務ではない） |

**教訓は 2 つです。**

1. **「確認済み」と書いた事実にも期限がある。** 法令の施行日は、**施行の直前に動くことがあります**
2. **レビュー担当が「あなたの記録は間違っている」と言ってきたら、記憶で退けず一次資料に当たること。**この件は、そうしなければ誤りのまま残っていました

教本ではこの延期を消さず、**「ハードローは作るのに時間がかかるだけでなく、作ったあとも予定どおりに動くとは限らない」**という節の主題の実例として書いてあります。

## 次にやること

順番に意味があります。

1. **残りの体験ウィジェットを作る（最優先）** … 一覧は `src/pages/Tools.tsx` の `GROUPS`。**10 個ぶんの id を並べてあるのに、実装は 3 個だけです。**先頭の「数と確率」は 0 個で、そのせいで体験ツールの画面が空で開く不具合が出ました（初期表示は暫定で回避してあります）。**`scripts/render-check.tsx` の `WIDGETS` にも足すこと**
2. **計算ドリルを作る** … `src/data/drills.ts`。**本番は 1 問 41 秒なので主役ではありません**
3. **シラバスと試験要項を見直す** … 第 1.4 版（2026 年 5 月 11 日改訂）を前提に書いてあります。**改訂されます。**確認した日付を、直したときに書き換えてください
4. **教本をもう一度、確認問題と突き合わせる** … 今回の確認問題レビューで、教本側の誤り（MYCIN の成績、part-of の反例、日本語の特殊音素）が 3 件見つかりました。**問題を直すと教本の誤りが出てくる**ので、逆向きの突き合わせにも価値があります

### デスクトップのランチャー

**`scripts/launch.cmd` → `scripts/launch.ps1`** の 2 段構えです。デスクトップのショートカット（`G検定 学習アプリ.lnk`）は `launch.cmd` を指しています。

- **`launch.cmd` は ASCII だけで書くこと。** cmd は `.cmd` を OEM コードページ（日本語環境では cp932）で読むので、日本語を書くと文字化けします。**表示する文字列は `launch.ps1` 側に置いてあります**
- **`launch.ps1` は BOM 付き UTF-8 で保存すること。** Windows PowerShell 5.1 は BOM のない `.ps1` を cp932 として読むため、**BOM を落とすと日本語の行で構文エラーになります**（実際に踏みました）
- ショートカットは `WScript.Shell` で作れます。**作成後は読み直して `IconLocation` を確認すること。** パスの `` などがエスケープとして消費され、壊れたパスが入ることがあります（これも実際に踏みました）

### 確認問題の構成（執筆済み）

**合計 435 問**です。**章ごとの比率は `src/data/categories.ts` の `questions` を保ち、その 3 倍**にしました。

| 章 | 問数 | 章 | 問数 |
| --- | --- | --- | --- |
| `a-what` | 15 | `g-seq` | 36 |
| `b-trend` | 33 | `h-recog` | 63 |
| `c-math` | 24 | `i-gen` | 51 |
| `d-ml` | 54 | `j-social` | 21 |
| `e-dl` | 45 | `k-law` | 36 |
| `f-layer` | 18 | `l-ethics` | 39 |

**なぜ 145 問ではないのか。** `Mock.tsx` は章の重み（合計 145）で母集団から抽出します。**母集団がちょうど 145 問だと、本番形式の模試が毎回まったく同じ 145 問になります。**3 倍あれば重複がほぼなくなり、節ごとの演習も成り立ちます（`itpassport_exam_app` は 100 問の試験に対して 334 問を用意していました）。

**書くときに `npm run check` が実際に捕まえたもの**（同じ型を繰り返さないこと）。

- **正解だけが突出して長い** … 11 問。読まずに「長いものを選ぶ」で当てられてしまう。**誤答を同じ密度に書き直した**
- **正解の位置の偏り** … 一度は「エ」が 288 問中 3 問、別の時点では「イ」が 288 問中 101 問。**`scripts/` の並べ替えで均した**（最終は ア 113 / イ 110 / ウ 108 / エ 104）。**ただし数値や順序が選択肢になっている問題は並べ替えない**（昇順・時系列に意味があるため）

### 教本を書いたときに追加した検査

`scripts/check.ts` に、実際に踏んだ誤りの型を検査として足してあります。**同じ型を繰り返さないため、消さないでください。**

- **問題文の長さ**（空白を除いて 120 字で注意・160 字でエラー）
- **節の骨格**（`# ざっくり言うと` / `# この節のまとめ` / `> **試験のポイント**` の有無、一問一答の数）
- **リンクのラベルと飛び先の食い違い**（ラベルが別の節のタイトルと一致しているのに、その節を指していない）
- **章を指すラベルで節へリンクしている**（章の扉へは飛べない）
- **日本語以外の文字体系の混入**（ハングル・キリル文字。「データ」の「デ」がハングルに、「短い」がキリル文字になっていた例が実際にあった。**ギリシャ文字は αβ 法・ε-greedy・β-VAE のように正当に使うので対象外**）
- **図の題の「N つ」と実際の数の食い違い**（`compare` は左右の見出し、`tree` は階層、その他は要素数で数える）。**レビュー担当が見落とした 1 件を、この検査が捕まえました**

**`compare` 図の誤用（1 対 1 の対応表を compare で書く）は、機械的に捕まえられません。** 要素数が偶数なら検査を通ってしまいます。**`compare` は「偶数行が左・奇数行が右」であることを、書くたびに思い出してください。**
